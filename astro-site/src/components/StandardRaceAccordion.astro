---
// スタンダード予想ページ用レースアコーディオンコンポーネント
import RaceStrategy from './RaceStrategy.astro';

export interface Props {
    raceData: any;
    raceNumber: string;
    isMainRace?: boolean;
}

const { raceData, raceNumber, isMainRace = false } = Astro.props;
const { raceInfo, horses, strategies, analysis } = raceData;

// アコーディオンIDの生成
const accordionId = `race-${raceNumber.toLowerCase()}-accordion`;
const toggleFunction = `toggleRace${raceNumber}()`;
---

<div class={`race-accordion ${isMainRace ? 'main-race' : ''}`}>
    <button class={`race-accordion-header race-${raceNumber.toLowerCase()}-header`} onclick={toggleFunction}>
        <div class="race-info">
            <div class="race-title">
                {isMainRace && <span class="main-race-badge">メイン</span>}
                {raceInfo.title}
            </div>
            <div class="race-meta">{raceInfo.distance} | {raceInfo.startTime}発走 | {raceInfo.horseCount}頭</div>
        </div>
        <div class="race-stats">
            <div class="expected-return">期待回収率 {raceInfo.expectedReturn}%</div>
            <div class="recommendation-badge">{raceInfo.recommendation}</div>
        </div>
        <div class="accordion-toggle">
            <span>展開</span>
            <div class="toggle-icon">▼</div>
        </div>
    </button>
    
    <div class={`race-accordion-content race-${raceNumber.toLowerCase()}-content`} id={accordionId}>
        <div class="race-detail-content">
            <!-- レース詳細情報 -->
            <div class="race-overview">
                <div class="race-conditions">
                    <h4>🏁 レース条件</h4>
                    <p>{raceInfo.raceDetails || `${raceInfo.track} ${raceInfo.raceNumber} ${raceInfo.distance} ${raceInfo.raceName}`}</p>
                </div>
            </div>

            <!-- 本命馬情報 -->
            <div class="main-horses-section">
                <h3>🏆 AI選出馬</h3>
                <div class="horses-grid">
                    <div class="horse-card main">
                        <div class="horse-header">
                            <span class="horse-number">{horses.main.number}</span>
                            <div class="horse-info">
                                <div class="horse-name">{horses.main.name}</div>
                                <div class="horse-type">{horses.main.type}</div>
                            </div>
                            <div class="confidence-badge">
                                {horses.main.confidence}%
                            </div>
                        </div>
                        <div class="horse-factors">
                            {horses.main.factors.slice(0, 3).map(factor => (
                                <span class="factor-tag main">{factor}</span>
                            ))}
                        </div>
                    </div>

                    <div class="horse-card sub">
                        <div class="horse-header">
                            <span class="horse-number">{horses.sub.number}</span>
                            <div class="horse-info">
                                <div class="horse-name">{horses.sub.name}</div>
                                <div class="horse-type">{horses.sub.type}</div>
                            </div>
                            <div class="confidence-badge">
                                {horses.sub.confidence}%
                            </div>
                        </div>
                        <div class="horse-factors">
                            {horses.sub.factors.slice(0, 3).map(factor => (
                                <span class="factor-tag sub">{factor}</span>
                            ))}
                        </div>
                    </div>

                    <div class="horse-card alt">
                        <div class="horse-header">
                            <span class="horse-number">{horses.sub1.number}</span>
                            <div class="horse-info">
                                <div class="horse-name">{horses.sub1.name}</div>
                                <div class="horse-type">{horses.sub1.type}</div>
                            </div>
                            <div class="confidence-badge">
                                {horses.sub1.confidence}%
                            </div>
                        </div>
                        <div class="horse-factors">
                            {horses.sub1.factors?.slice(0, 2).map(factor => (
                                <span class="factor-tag alt">{factor}</span>
                            ))}
                        </div>
                    </div>

                    <div class="horse-card alt">
                        <div class="horse-header">
                            <span class="horse-number">{horses.sub2.number}</span>
                            <div class="horse-info">
                                <div class="horse-name">{horses.sub2.name}</div>
                                <div class="horse-type">{horses.sub2.type}</div>
                            </div>
                            <div class="confidence-badge">
                                {horses.sub2.confidence}%
                            </div>
                        </div>
                        <div class="horse-factors">
                            {horses.sub2.factors?.slice(0, 2).map(factor => (
                                <span class="factor-tag alt">{factor}</span>
                            ))}
                        </div>
                    </div>
                </div>
            </div>

            <!-- 投資戦略 -->
            <div class="strategies-section">
                <h3>📊 投資戦略</h3>
                <div class="strategies-grid">
                    <RaceStrategy 
                        strategy={strategies.safe} 
                        strategyType="safe" 
                        icon="🎯" 
                    />
                    <RaceStrategy 
                        strategy={strategies.balance} 
                        strategyType="balance" 
                        icon="⚖️" 
                    />
                    <RaceStrategy 
                        strategy={strategies.aggressive} 
                        strategyType="aggressive" 
                        icon="🚀" 
                    />
                </div>
            </div>

            <!-- AI分析 -->
            <div class="analysis-section">
                <h3>🤖 AI分析</h3>
                <div class="analysis-content">
                    <div class="race-prediction">
                        <h4>レース展開予測</h4>
                        <p>{analysis.raceExpected}</p>
                    </div>
                    <div class="key-indicators">
                        <h4>重要指標</h4>
                        <div class="indicators-row">
                            <div class="indicator">
                                <span class="label">精度</span>
                                <span class="value">{analysis.keyIndicators.accuracy}%</span>
                            </div>
                            <div class="indicator">
                                <span class="label">類似レース</span>
                                <span class="value">{analysis.keyIndicators.similarRaces}</span>
                            </div>
                            <div class="indicator">
                                <span class="label">信頼区間</span>
                                <span class="value">{analysis.keyIndicators.confidenceInterval}%</span>
                            </div>
                            <div class="indicator">
                                <span class="label">推奨投資</span>
                                <span class="value">{analysis.keyIndicators.recommendedInvestment}</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .race-accordion {
        margin-bottom: 25px;
        border-radius: 16px;
        overflow: hidden;
        background: rgba(30, 41, 59, 0.4);
        border: 1px solid rgba(16, 185, 129, 0.3);
        backdrop-filter: blur(10px);
    }

    .race-accordion.main-race {
        border: 2px solid rgba(16, 185, 129, 0.6);
        box-shadow: 0 8px 32px rgba(16, 185, 129, 0.25);
    }

    .race-accordion-header {
        width: 100%;
        padding: 25px;
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
        border: none;
        color: white;
        cursor: pointer;
        display: flex;
        justify-content: space-between;
        align-items: center;
        transition: all 0.3s;
        text-align: left;
    }

    .race-accordion.main-race .race-accordion-header {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(59, 130, 246, 0.15) 100%);
    }

    .race-accordion-header:hover {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(59, 130, 246, 0.2) 100%);
    }

    .race-accordion-header.active .toggle-icon {
        transform: rotate(180deg);
    }

    .race-info {
        flex: 1;
    }

    .race-title {
        font-size: 1.3rem;
        font-weight: 700;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .main-race-badge {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        color: white;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 800;
        box-shadow: 0 2px 8px rgba(16, 185, 129, 0.3);
    }

    .race-meta {
        color: #94a3b8;
        font-size: 0.95rem;
        font-weight: 500;
    }

    .race-stats {
        display: flex;
        align-items: center;
        gap: 20px;
        text-align: right;
    }

    .confidence-score, .expected-return {
        font-size: 0.9rem;
        color: #10b981;
        font-weight: 600;
        text-align: center;
    }

    .recommendation-badge {
        background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
        color: white;
        padding: 6px 12px;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.9rem;
    }

    .accordion-toggle {
        display: flex;
        align-items: center;
        gap: 8px;
        color: #3b82f6;
        font-weight: 600;
        margin-left: 20px;
    }

    .toggle-icon {
        transition: transform 0.3s;
        font-size: 1.2rem;
    }

    .race-accordion-content {
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.4s ease-out;
        background: rgba(15, 23, 42, 0.6);
    }

    .race-accordion-content.active {
        max-height: 3000px;
    }

    .race-detail-content {
        padding: 30px;
    }

    .race-overview {
        margin-bottom: 30px;
        padding: 20px;
        background: rgba(59, 130, 246, 0.1);
        border-radius: 12px;
        border: 1px solid rgba(59, 130, 246, 0.2);
    }

    .race-conditions h4 {
        color: #3b82f6;
        margin-bottom: 10px;
        font-size: 1.1rem;
    }

    .race-conditions p {
        color: #e2e8f0;
        line-height: 1.6;
    }

    .main-horses-section {
        margin-bottom: 35px;
    }

    .main-horses-section h3 {
        color: #e2e8f0;
        margin-bottom: 20px;
        font-size: 1.3rem;
        font-weight: 700;
    }

    .horses-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 20px;
    }

    .horse-card {
        background: rgba(30, 41, 59, 0.8);
        border: 2px solid rgba(148, 163, 184, 0.3);
        border-radius: 12px;
        padding: 20px;
        transition: all 0.3s;
    }

    .horse-card.main {
        border-color: rgba(16, 185, 129, 0.5);
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(16, 185, 129, 0.05) 100%);
    }

    .horse-card.sub {
        border-color: rgba(59, 130, 246, 0.5);
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(59, 130, 246, 0.05) 100%);
    }

    .horse-card.alt {
        border-color: rgba(245, 158, 11, 0.5);
        background: linear-gradient(135deg, rgba(245, 158, 11, 0.1) 0%, rgba(245, 158, 11, 0.05) 100%);
    }

    .horse-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    }

    .horse-header {
        display: flex;
        align-items: center;
        gap: 15px;
        margin-bottom: 15px;
    }

    .horse-number {
        font-size: 2rem;
        font-weight: 900;
        min-width: 50px;
        text-align: center;
    }

    .horse-card.main .horse-number {
        color: #10b981;
        text-shadow: 0 2px 4px rgba(16, 185, 129, 0.3);
    }

    .horse-card.sub .horse-number {
        color: #3b82f6;
        text-shadow: 0 2px 4px rgba(59, 130, 246, 0.3);
    }

    .horse-card.alt .horse-number {
        color: #f59e0b;
        text-shadow: 0 2px 4px rgba(245, 158, 11, 0.3);
    }

    .horse-info {
        flex: 1;
    }

    .horse-name {
        font-size: 1.1rem;
        font-weight: 700;
        color: #e2e8f0;
        margin-bottom: 4px;
    }

    .horse-type {
        font-size: 0.9rem;
        color: #94a3b8;
        font-weight: 600;
    }

    .confidence-badge {
        background: rgba(251, 191, 36, 0.2);
        color: #fbbf24;
        padding: 6px 10px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 700;
        border: 1px solid rgba(251, 191, 36, 0.3);
    }

    .horse-factors {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }

    .factor-tag {
        padding: 4px 10px;
        border-radius: 15px;
        font-size: 0.8rem;
        font-weight: 500;
        border: 1px solid;
    }

    .factor-tag.main {
        background: rgba(16, 185, 129, 0.2);
        color: #10b981;
        border-color: rgba(16, 185, 129, 0.3);
    }

    .factor-tag.sub {
        background: rgba(59, 130, 246, 0.2);
        color: #3b82f6;
        border-color: rgba(59, 130, 246, 0.3);
    }

    .factor-tag.alt {
        background: rgba(245, 158, 11, 0.2);
        color: #f59e0b;
        border-color: rgba(245, 158, 11, 0.3);
    }

    .strategies-section {
        margin-bottom: 35px;
    }

    .strategies-section h3 {
        color: #e2e8f0;
        margin-bottom: 20px;
        font-size: 1.3rem;
        font-weight: 700;
    }

    .strategies-grid {
        display: grid;
        gap: 20px;
    }

    .analysis-section {
        background: rgba(16, 185, 129, 0.08);
        border: 1px solid rgba(16, 185, 129, 0.2);
        border-radius: 12px;
        padding: 25px;
    }

    .analysis-section h3 {
        color: #10b981;
        margin-bottom: 20px;
        font-size: 1.3rem;
        font-weight: 700;
    }

    .race-prediction {
        margin-bottom: 25px;
    }

    .race-prediction h4 {
        color: #3b82f6;
        margin-bottom: 10px;
        font-size: 1.1rem;
    }

    .race-prediction p {
        color: #e2e8f0;
        line-height: 1.7;
    }

    .key-indicators h4 {
        color: #3b82f6;
        margin-bottom: 15px;
        font-size: 1.1rem;
    }

    .indicators-row {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 15px;
    }

    .indicator {
        background: rgba(59, 130, 246, 0.1);
        padding: 12px;
        border-radius: 8px;
        text-align: center;
        border: 1px solid rgba(59, 130, 246, 0.2);
    }

    .indicator .label {
        display: block;
        color: #94a3b8;
        font-size: 0.85rem;
        margin-bottom: 4px;
    }

    .indicator .value {
        color: #3b82f6;
        font-weight: 700;
        font-size: 1rem;
    }

    /* Mobile Responsive */
    @media (max-width: 768px) {
        .race-accordion-header {
            padding: 20px 15px;
            flex-direction: column;
            gap: 15px;
            text-align: center;
        }

        .race-stats {
            justify-content: center;
            gap: 15px;
        }

        .horses-grid {
            grid-template-columns: 1fr;
        }

        .horse-header {
            gap: 12px;
        }

        .horse-number {
            font-size: 1.8rem;
            min-width: 40px;
        }

        .indicators-row {
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
        }
    }

    @media (max-width: 480px) {
        .race-detail-content {
            padding: 20px 15px;
        }

        .race-title {
            font-size: 1.1rem;
        }

        .indicators-row {
            grid-template-columns: 1fr;
        }
    }
</style>