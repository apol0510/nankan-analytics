---
// レースアコーディオンの共通コンポーネント
import RaceStrategy from './RaceStrategy.astro';

export interface Props {
    raceData: any;
    raceNumber: string;
    accordionId: string;
}

const { raceData, raceNumber, accordionId } = Astro.props;
const { raceInfo, horses, strategies, analysis } = raceData;
---

<div class="race-accordion">
    <button class={`race-accordion-header race-${raceNumber.toLowerCase()}-header`} onclick={`toggleRace${raceNumber}()`}>
        <div class="race-info">
            <div class="race-title">{raceInfo.title}</div>
            <div class="race-meta">{raceInfo.distance} | {raceInfo.startTime}発走</div>
        </div>
        <div class="race-stats">
            <div class="expected-return">期待回収率 {raceInfo.expectedReturn}%</div>
        </div>
        <div class="accordion-toggle">
            <span>展開</span>
            <div class="toggle-icon">▼</div>
        </div>
    </button>
    
    <div class={`race-accordion-content race-${raceNumber.toLowerCase()}-content`} id={accordionId}>
        <div class="race-detail-content">
            <!-- 本命馬情報 -->
            <div class="main-horse-section">
                <h3>🏆 本命・対抗</h3>
                <div class="horse-grid">
                    <div class="horse-card main">
                        <div class="horse-number">{horses.main.number}</div>
                        <div class="horse-name">{horses.main.name}</div>
                        <div class="horse-type">{horses.main.type}</div>
                    </div>
                    <div class="horse-card sub">
                        <div class="horse-number">{horses.sub.number}</div>
                        <div class="horse-name">{horses.sub.name}</div>
                        <div class="horse-type">{horses.sub.type}</div>
                    </div>
                </div>
            </div>

            <!-- 投資戦略 -->
            <div class="strategies-section">
                <h3>📊 投資戦略</h3>
                <div class="strategies-grid">
                    <RaceStrategy 
                        strategy={strategies.safe} 
                        strategyType="safe" 
                        icon="🎯" 
                    />
                    <RaceStrategy 
                        strategy={strategies.balance} 
                        strategyType="balance" 
                        icon="⚖️" 
                    />
                    <RaceStrategy 
                        strategy={strategies.aggressive} 
                        strategyType="aggressive" 
                        icon="🚀" 
                    />
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .race-accordion {
        margin-bottom: 20px;
        border-radius: 12px;
        overflow: hidden;
        background: rgba(30, 41, 59, 0.4);
        border: 1px solid rgba(59, 130, 246, 0.3);
    }

    .race-accordion-header {
        width: 100%;
        padding: 20px;
        background: rgba(30, 41, 59, 0.8);
        border: none;
        color: white;
        cursor: pointer;
        display: flex;
        justify-content: space-between;
        align-items: center;
        transition: all 0.3s;
    }

    .race-accordion-header:hover {
        background: rgba(59, 130, 246, 0.2);
    }

    .race-info {
        text-align: left;
    }

    .race-title {
        font-size: 1.2rem;
        font-weight: 700;
        margin-bottom: 4px;
    }

    .race-meta {
        color: #94a3b8;
        font-size: 0.9rem;
    }

    .race-stats {
        display: flex;
        gap: 20px;
        text-align: right;
    }

    .confidence-score, .expected-return {
        font-size: 0.9rem;
        color: #10b981;
        font-weight: 600;
    }

    .accordion-toggle {
        display: flex;
        align-items: center;
        gap: 8px;
        color: #3b82f6;
    }

    .toggle-icon {
        transition: transform 0.3s;
    }

    .race-accordion-header.active .toggle-icon {
        transform: rotate(180deg);
    }

    .race-accordion-content {
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease;
    }

    .race-accordion-content.active {
        max-height: 2000px;
    }

    .race-detail-content {
        padding: 20px;
    }

    .main-horse-section {
        margin-bottom: 30px;
    }

    .horse-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 15px;
    }

    .horse-card {
        background: rgba(59, 130, 246, 0.1);
        border: 1px solid rgba(59, 130, 246, 0.3);
        border-radius: 8px;
        padding: 15px;
        text-align: center;
    }

    .horse-card.main {
        background: rgba(16, 185, 129, 0.1);
        border-color: rgba(16, 185, 129, 0.3);
    }

    .horse-number {
        font-size: 1.5rem;
        font-weight: 800;
        color: #3b82f6;
        margin-bottom: 5px;
    }

    .horse-card.main .horse-number {
        color: #10b981;
    }

    .horse-name {
        font-weight: 700;
        color: white;
        margin-bottom: 5px;
    }

    .horse-type {
        color: #94a3b8;
        font-size: 0.9rem;
        margin-bottom: 5px;
    }

    .confidence {
        color: #fbbf24;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .strategies-section h3 {
        color: #e2e8f0;
        margin-bottom: 20px;
        font-size: 1.2rem;
    }

    .strategies-grid {
        display: grid;
        gap: 20px;
    }

    @media (max-width: 768px) {
        .race-accordion-header {
            flex-direction: column;
            gap: 15px;
            text-align: center;
        }

        .race-stats {
            justify-content: center;
        }
    }
</style>