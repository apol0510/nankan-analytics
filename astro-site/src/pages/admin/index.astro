---
// admin/ アクセス時は dashboard にリダイレクト
export const prerender = true;

// サーバーサイドリダイレクト
if (Astro.request) {
  return Astro.redirect('/admin/dashboard', 301);
}
---

<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>管理画面 - リダイレクト中</title>
  <meta http-equiv="refresh" content="0; url=/admin/dashboard">
</head>
<body>
  <script>
    // クライアントサイドリダイレクト（フォールバック）
    window.location.href = '/admin/dashboard';
  </script>
  <p>管理画面ダッシュボードにリダイレクト中...</p>
  <p>自動的にリダイレクトされない場合は <a href="/admin/dashboard">こちら</a> をクリックしてください。</p>
</body>
</html>
