---
// 配配メール メルマガ配信管理画面
---

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>メルマガ配信管理 | NANKANアナリティクス</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #e2e8f0;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            background: rgba(30, 41, 59, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 16px;
            padding: 30px;
            margin-bottom: 30px;
            text-align: center;
        }

        .header h1 {
            color: #3b82f6;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .section {
            background: rgba(30, 41, 59, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 16px;
            padding: 30px;
            margin-bottom: 30px;
        }

        .section h2 {
            color: #3b82f6;
            font-size: 22px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            color: #cbd5e1;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .form-input, .form-select, .form-textarea {
            width: 100%;
            padding: 12px 16px;
            background: rgba(15, 23, 42, 0.8);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 8px;
            color: #e2e8f0;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .form-textarea {
            min-height: 200px;
            resize: vertical;
            font-family: inherit;
        }

        .form-input:focus, .form-select:focus, .form-textarea:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .btn {
            padding: 12px 24px;
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-right: 10px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px -5px rgba(59, 130, 246, 0.4);
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .btn-secondary {
            background: rgba(148, 163, 184, 0.8);
            color: #e2e8f0;
        }

        .btn-secondary:hover {
            background: rgba(148, 163, 184, 1);
            box-shadow: 0 10px 25px -5px rgba(148, 163, 184, 0.4);
        }

        .success-message, .error-message, .info-message {
            padding: 12px 16px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .success-message {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            color: #86efac;
        }

        .error-message {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
            color: #fca5a5;
        }

        .info-message {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.3);
            color: #93c5fd;
        }

        .checkbox-group {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 20px;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .checkbox-item input[type="checkbox"] {
            width: 18px;
            height: 18px;
            accent-color: #3b82f6;
        }

        .checkbox-item label {
            color: #cbd5e1;
            font-size: 14px;
            cursor: pointer;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(59, 130, 246, 0.2);
            border-radius: 12px;
            padding: 20px;
        }

        .stat-value {
            font-size: 24px;
            font-weight: 700;
            color: #3b82f6;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 14px;
            color: #94a3b8;
        }

        .back-link {
            display: inline-block;
            color: #3b82f6;
            text-decoration: none;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .loading {
            display: none;
            align-items: center;
            justify-content: center;
            margin-top: 10px;
        }

        .spinner {
            width: 20px;
            height: 20px;
            border: 2px solid #475569;
            border-top: 2px solid #3b82f6;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-right: 10px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="/admin" class="back-link">← 管理画面に戻る</a>
        
        <div class="header">
            <h1>📧 メルマガ配信管理</h1>
            <p>配配メールを使用した会員ランク別メルマガ配信</p>
        </div>

        <div id="message-container"></div>

        <!-- 統計情報 -->
        <div class="section">
            <h2>📊 配信先統計</h2>
            <div class="stats-grid" id="stats-grid">
                <div class="stat-card">
                    <div class="stat-value" id="free-count">-</div>
                    <div class="stat-label">無料会員</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="standard-count">-</div>
                    <div class="stat-label">Standard会員</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="premium-count">-</div>
                    <div class="stat-label">Premium会員</div>
                </div>
            </div>
            <button class="btn btn-secondary" onclick="loadStats()">統計を更新</button>
        </div>

        <!-- メルマガ配信 -->
        <div class="section">
            <h2>📬 メルマガ配信</h2>
            
            <form id="newsletter-form">
                <div class="form-group">
                    <label class="form-label">配信対象グループ</label>
                    <div class="checkbox-group">
                        <div class="checkbox-item">
                            <input type="checkbox" id="target-free" name="target-groups" value="free" checked>
                            <label for="target-free">無料会員</label>
                        </div>
                        <div class="checkbox-item">
                            <input type="checkbox" id="target-standard" name="target-groups" value="standard" checked>
                            <label for="target-standard">Standard会員</label>
                        </div>
                        <div class="checkbox-item">
                            <input type="checkbox" id="target-premium" name="target-groups" value="premium" checked>
                            <label for="target-premium">Premium会員</label>
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label" for="subject">件名</label>
                    <input type="text" id="subject" name="subject" class="form-input" required 
                           placeholder="例: 【NANKAN】今週の注目レース予想配信">
                </div>

                <div class="form-group">
                    <label class="form-label" for="content-type">コンテンツタイプ</label>
                    <select id="content-type" name="contentType" class="form-select">
                        <option value="html">HTML形式</option>
                        <option value="text">テキスト形式</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label" for="content">メール内容</label>
                    <textarea id="content" name="content" class="form-textarea" required 
                              placeholder="メールの内容を入力してください..."></textarea>
                </div>

                <button type="submit" class="btn" id="send-btn">
                    下書きとして保存
                </button>
                <button type="button" class="btn btn-secondary" onclick="previewNewsletter()">
                    プレビュー
                </button>
                <button type="button" class="btn btn-secondary" onclick="resetForm()">
                    リセット
                </button>

                <div class="loading" id="loading">
                    <div class="spinner"></div>
                    <span>配信中...</span>
                </div>
            </form>
        </div>

        <!-- API接続テスト -->
        <div class="section">
            <h2>🔧 配配メール接続テスト</h2>
            <p style="margin-bottom: 20px; color: #94a3b8;">配配メールAPIとの接続状況を確認できます。</p>
            <button class="btn btn-secondary" onclick="testConnection()" id="test-btn">
                接続テスト実行
            </button>
            <div id="test-result"></div>
        </div>
    </div>

    <script type="module">
        const form = document.getElementById('newsletter-form');
        const sendBtn = document.getElementById('send-btn');
        const loading = document.getElementById('loading');
        const messageContainer = document.getElementById('message-container');

        // フォーム送信処理
        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const targetGroups = Array.from(document.querySelectorAll('input[name="target-groups"]:checked'))
                .map(cb => cb.value);
            
            if (targetGroups.length === 0) {
                showMessage('配信対象グループを選択してください。', 'error');
                return;
            }

            const formData = {
                targetGroups,
                subject: document.getElementById('subject').value,
                content: document.getElementById('content').value,
                contentType: document.getElementById('content-type').value
            };

            setLoading(true);
            clearMessages();

            try {
                const response = await fetch('/api/haihai-mail/send-newsletter', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(formData)
                });

                const result = await response.json();

                if (response.ok && result.success) {
                    showMessage('メルマガの下書きを保存しました！配配メール管理画面から配信予約を行ってください。', 'success');
                    resetForm();
                } else {
                    throw new Error(result.error || '配信に失敗しました');
                }
            } catch (error) {
                console.error('Newsletter send error:', error);
                showMessage(error.message || 'メルマガ配信に失敗しました。', 'error');
            } finally {
                setLoading(false);
            }
        });

        // 統計情報を読み込み
        window.loadStats = async function() {
            try {
                const response = await fetch('/api/haihai-mail/get-stats');
                const result = await response.json();

                if (response.ok && result.success) {
                    document.getElementById('free-count').textContent = result.data.free || '0';
                    document.getElementById('standard-count').textContent = result.data.standard || '0';
                    document.getElementById('premium-count').textContent = result.data.premium || '0';
                } else {
                    console.warn('統計取得に失敗:', result.error);
                }
            } catch (error) {
                console.error('統計取得エラー:', error);
            }
        };

        // 接続テスト
        window.testConnection = async function() {
            const testBtn = document.getElementById('test-btn');
            const testResult = document.getElementById('test-result');
            
            testBtn.disabled = true;
            testBtn.textContent = 'テスト中...';
            
            try {
                const response = await fetch('/api/haihai-mail/test-connection');
                const result = await response.json();
                
                if (result.success) {
                    testResult.innerHTML = '<div class="success-message">✅ 配配メールAPIに正常に接続できました</div>';
                } else {
                    testResult.innerHTML = `<div class="error-message">❌ 接続エラー: ${result.message}</div>`;
                }
            } catch (error) {
                testResult.innerHTML = `<div class="error-message">❌ 接続テストに失敗しました: ${error.message}</div>`;
            } finally {
                testBtn.disabled = false;
                testBtn.textContent = '接続テスト実行';
            }
        };

        // プレビュー機能
        window.previewNewsletter = function() {
            const subject = document.getElementById('subject').value;
            const content = document.getElementById('content').value;
            const contentType = document.getElementById('content-type').value;
            
            if (!subject || !content) {
                showMessage('件名と内容を入力してください。', 'error');
                return;
            }

            const previewWindow = window.open('', 'preview', 'width=600,height=800');
            const previewContent = contentType === 'html' ? content : content.replace(/\n/g, '<br>');
            
            previewWindow.document.write(`
                <html>
                <head>
                    <title>プレビュー: ${subject}</title>
                    <style>
                        body { font-family: Arial, sans-serif; padding: 20px; }
                        .preview-header { background: #f0f0f0; padding: 15px; margin-bottom: 20px; }
                    </style>
                </head>
                <body>
                    <div class="preview-header">
                        <h2>件名: ${subject}</h2>
                        <p>コンテンツタイプ: ${contentType.toUpperCase()}</p>
                    </div>
                    <div class="preview-content">
                        ${previewContent}
                    </div>
                </body>
                </html>
            `);
        };

        // フォームリセット
        window.resetForm = function() {
            form.reset();
            document.querySelectorAll('input[name="target-groups"]').forEach(cb => cb.checked = true);
        };

        // ヘルパー関数
        function setLoading(isLoading) {
            if (isLoading) {
                sendBtn.disabled = true;
                sendBtn.style.display = 'none';
                loading.style.display = 'flex';
            } else {
                sendBtn.disabled = false;
                sendBtn.style.display = 'inline-block';
                loading.style.display = 'none';
            }
        }

        function showMessage(message, type = 'info') {
            const messageDiv = document.createElement('div');
            messageDiv.className = `${type}-message`;
            messageDiv.textContent = message;
            messageContainer.appendChild(messageDiv);
            
            // 5秒後に自動削除
            setTimeout(() => {
                if (messageDiv.parentNode) {
                    messageDiv.parentNode.removeChild(messageDiv);
                }
            }, 5000);
        }

        function clearMessages() {
            messageContainer.innerHTML = '';
        }

        // 初期統計読み込み
        loadStats();
    </script>
</body>
</html>