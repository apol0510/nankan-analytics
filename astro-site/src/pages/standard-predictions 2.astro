---
import BaseLayout from '../layouts/BaseLayout.astro';
import allRacesData from '../data/allRacesPrediction.json';

// スタンダードプランは9R-11Rにアクセス可能
const standardRaces = allRacesData.races.filter(race => 
    race.tier === 'free' || race.tier === 'standard'
);
const totalRaces = standardRaces.length;
---

<BaseLayout title="スタンダード予想 | NANKANアナリティクス" description="スタンダード会員限定の後半3レース（10R-12R）AI予想。仕事帰りの勝負レースで確実に利益を狙う。">
    <div class="container">
        <div class="page-header">
            <h1 class="page-title">🏆 スタンダード予想</h1>
            <p class="page-subtitle">
                後半3レース（10R-12R）のAI予想を提供<br>
                仕事帰りの勝負レースで確実に利益を狙う
            </p>
            <div class="plan-badge">STANDARD PLAN</div>
        </div>

        <!-- ログイン状態確認 -->
        <div id="loading-state" class="loading-state">
            <div class="spinner"></div>
            アクセス権限を確認中...
        </div>

        <!-- アクセス拒否 -->
        <div id="access-denied" class="access-denied" style="display: none;">
            <div class="access-denied-icon">🔐</div>
            <h2 class="access-denied-title">スタンダード会員限定コンテンツ</h2>
            <p class="access-denied-message">
                このコンテンツをご覧いただくには、スタンダード会員への登録が必要です。<br>
                後半3レース（10R-12R）の精密なAI予想をお楽しみください。
            </p>
            <a href="/pricing" class="btn">スタンダード会員になる</a>
        </div>

        <!-- スタンダードコンテンツ -->
        <div id="standard-content" style="display: none;">
            <!-- 今日の成績 -->
            <div class="today-stats">
                <div class="stats-title">
                    📊 本日の成績（スタンダード限定レース）
                </div>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-number">2</div>
                        <div class="stat-desc">的中</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">3</div>
                        <div class="stat-desc">レース</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">66.7%</div>
                        <div class="stat-desc">的中率</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number">142%</div>
                        <div class="stat-desc">回収率</div>
                    </div>
                </div>
            </div>

            <div id="predictions-container">
                <div class="loading-state">
                    <div class="spinner"></div>
                    本日のスタンダード予想を読み込み中...
                </div>
            </div>

            <!-- プレミアムアップセル -->
            <div class="premium-upsell">
                <div class="upsell-title">👑 さらなる利益を狙うなら</div>
                <p class="upsell-text">
                    プレミアム会員なら全12レースの予想と3つの投資戦略で、<br>
                    より多くの利益機会を逃しません。
                </p>
                <div class="upsell-features">
                    <div class="upsell-feature">
                        <span class="feature-icon">✅</span>
                        全レース予想（1R-12R）
                    </div>
                    <div class="upsell-feature">
                        <span class="feature-icon">✅</span>
                        3つの投資戦略
                    </div>
                    <div class="upsell-feature">
                        <span class="feature-icon">✅</span>
                        高配当狙い戦略
                    </div>
                    <div class="upsell-feature">
                        <span class="feature-icon">✅</span>
                        前日夜の早期配信
                    </div>
                </div>
                <a href="/pricing" class="btn">プレミアムにアップグレード</a>
            </div>
        </div>
    </div>
</BaseLayout>

<style>
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    .page-header {
        text-align: center;
        margin-bottom: 40px;
    }

    .page-title {
        font-size: 2.5rem;
        font-weight: 800;
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 10px;
    }

    .page-subtitle {
        color: #94a3b8;
        font-size: 1.1rem;
        margin-bottom: 30px;
    }

    .plan-badge {
        display: inline-block;
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        color: white;
        padding: 8px 24px;
        border-radius: 20px;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .prediction-grid {
        display: grid;
        gap: 30px;
        margin-top: 40px;
    }

    .race-card {
        margin-bottom: 40px;
    }

    /* レースヘッダー（free-prediction風） */
    .race-header-section {
        background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
        padding: 30px;
        border-radius: 12px;
        margin-bottom: 30px;
        border: 1px solid rgba(59, 130, 246, 0.2);
        text-align: center;
    }

    .ai-badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.3) 100%);
        padding: 6px 16px;
        border-radius: 20px;
        border: 1px solid rgba(16, 185, 129, 0.3);
        margin-bottom: 15px;
    }

    .pulse {
        width: 8px;
        height: 8px;
        background: #ef4444;
        border-radius: 50%;
        animation: pulse 2s infinite;
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }

    .ai-status {
        color: #10b981;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .race-title {
        font-size: 2rem;
        font-weight: 800;
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 10px;
    }

    .ai-metrics {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 15px;
        margin-top: 20px;
    }

    .metric {
        background: rgba(255, 255, 255, 0.05);
        padding: 15px;
        border-radius: 8px;
        text-align: center;
    }

    .metric-value {
        font-size: 1.8rem;
        font-weight: 700;
        color: #10b981;
    }

    .metric-label {
        font-size: 0.85rem;
        color: #64748b;
        margin-top: 5px;
    }

    /* 馬情報セクション（free-prediction風） */
    .analysis-section {
        background: rgba(255, 255, 255, 0.05);
        padding: 25px;
        border-radius: 12px;
        margin-bottom: 25px;
        border: 1px solid rgba(148, 163, 184, 0.1);
    }

    .section-title {
        font-size: 1.3rem;
        font-weight: 700;
        color: #3b82f6;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    /* 馬カード（free-prediction風） */
    .horse-card {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(139, 92, 246, 0.05) 100%);
        padding: 20px;
        border-radius: 10px;
        margin-bottom: 15px;
        border: 1px solid rgba(59, 130, 246, 0.2);
        position: relative;
    }

    /* ◎印（本命）- 最大サイズ */
    .horse-card-main {
        padding: 30px;
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
        border: 2px solid rgba(16, 185, 129, 0.3);
        box-shadow: 0 4px 15px rgba(16, 185, 129, 0.2);
    }

    /* ○印（対抗）- 中サイズ */
    .horse-card-sub {
        padding: 25px;
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.08) 0%, rgba(139, 92, 246, 0.08) 100%);
        border: 1.5px solid rgba(59, 130, 246, 0.25);
    }

    .horse-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 15px;
    }

    /* 馬券印マーク */
    .horse-mark-main {
        display: inline-block;
        font-size: 1.8rem;
        color: #10b981;
        font-weight: 900;
        margin-right: 10px;
    }

    .horse-mark-sub {
        display: inline-block;
        font-size: 1.5rem;
        color: #3b82f6;
        font-weight: 800;
        margin-right: 10px;
    }

    .horse-number {
        display: inline-block;
        width: 30px;
        height: 30px;
        background: #8b5cf6;
        color: white;
        text-align: center;
        line-height: 30px;
        border-radius: 50%;
        font-weight: 700;
        margin-right: 10px;
    }

    .horse-name {
        font-size: 1.2rem;
        font-weight: 700;
        color: #e2e8f0;
    }

    .horse-type {
        margin-left: 10px;
        font-weight: 600;
    }

    .confidence-score {
        background: linear-gradient(135deg, #f59e0b 0%, #fb923c 100%);
        padding: 5px 12px;
        border-radius: 15px;
        font-size: 0.9rem;
        font-weight: 600;
    }

    .ai-factors {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 10px;
        margin-top: 15px;
    }

    .factor {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 0.9rem;
        color: #e2e8f0;
    }

    .factor-icon {
        width: 20px;
        height: 20px;
        background: rgba(16, 185, 129, 0.2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #10b981;
        font-size: 12px;
    }

    /* 投資戦略セクション（free-prediction風） */
    .recommendation-section {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
        padding: 25px;
        border-radius: 12px;
        border: 1px solid rgba(16, 185, 129, 0.2);
        margin-bottom: 25px;
    }

    .bet-strategy {
        background: rgba(255, 255, 255, 0.05);
        padding: 20px;
        border-radius: 10px;
        margin-bottom: 20px;
    }

    .strategy-title {
        font-size: 1.1rem;
        font-weight: 700;
        color: #10b981;
        margin-bottom: 15px;
    }

    .bet-list {
        background: rgba(0, 0, 0, 0.2);
        padding: 15px;
        border-radius: 8px;
        margin-top: 10px;
    }

    .bet-item {
        display: flex;
        justify-content: space-between;
        padding: 8px 0;
        border-bottom: 1px solid rgba(148, 163, 184, 0.1);
    }

    .bet-item:last-child {
        border-bottom: none;
    }

    .bet-type {
        color: #3b82f6;
        font-weight: 600;
    }

    .bet-horses {
        color: #e2e8f0;
    }

    .bet-points {
        color: #10b981;
        font-weight: 600;
    }

    /* リスクインジケーター */
    .risk-indicator {
        display: flex;
        gap: 5px;
        margin-top: 15px;
    }

    .risk-bar {
        height: 8px;
        width: 60px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 4px;
    }

    .risk-bar.active.risk-low {
        background: #10b981;
    }

    .risk-bar.active.risk-medium {
        background: #f59e0b;
    }

    .risk-bar.active.risk-high {
        background: #ef4444;
    }

    .expected-return {
        background: rgba(16, 185, 129, 0.1);
        padding: 15px;
        border-radius: 8px;
        margin-top: 15px;
        text-align: center;
    }

    .return-value {
        font-size: 1.5rem;
        font-weight: 700;
        color: #10b981;
    }

    .loading-state {
        text-align: center;
        padding: 60px 20px;
        color: #94a3b8;
    }

    .spinner {
        width: 40px;
        height: 40px;
        border: 3px solid rgba(245, 158, 11, 0.3);
        border-top: 3px solid #f59e0b;
        border-radius: 50%;
        animation: spin 1s linear infinite;
        margin: 0 auto 20px;
    }

    @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }

    .no-data {
        text-align: center;
        padding: 60px 20px;
        background: rgba(245, 158, 11, 0.1);
        border: 2px dashed rgba(245, 158, 11, 0.3);
        border-radius: 16px;
        margin-top: 40px;
    }

    .no-data-icon {
        font-size: 3rem;
        margin-bottom: 20px;
    }

    .no-data-title {
        color: #f59e0b;
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 10px;
    }

    .no-data-text {
        color: #94a3b8;
        line-height: 1.6;
    }

    /* アクセス制御 */
    .access-denied {
        text-align: center;
        padding: 80px 40px;
        background: rgba(245, 158, 11, 0.1);
        border: 2px solid rgba(245, 158, 11, 0.3);
        border-radius: 16px;
        margin: 40px 0;
    }

    .access-denied-icon {
        font-size: 4rem;
        margin-bottom: 24px;
    }

    .access-denied-title {
        color: #f59e0b;
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 16px;
    }

    .access-denied-message {
        color: #94a3b8;
        line-height: 1.6;
        margin-bottom: 32px;
    }

    .btn {
        display: inline-block;
        background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        color: white;
        padding: 12px 32px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s;
    }

    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(245, 158, 11, 0.3);
    }

    /* 特典案内 */
    .premium-upsell {
        background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(168, 85, 247, 0.1) 100%);
        border: 2px solid rgba(139, 92, 246, 0.3);
        border-radius: 16px;
        padding: 30px;
        margin: 40px 0;
        text-align: center;
    }

    .upsell-title {
        color: #a855f7;
        font-size: 1.3rem;
        font-weight: 600;
        margin-bottom: 16px;
    }

    .upsell-text {
        color: #94a3b8;
        line-height: 1.6;
        margin-bottom: 24px;
    }

    .upsell-features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 16px;
        margin: 20px 0;
        text-align: left;
    }

    .upsell-feature {
        display: flex;
        align-items: center;
        gap: 12px;
        color: #e2e8f0;
        font-size: 0.9rem;
    }

    .feature-icon {
        color: #a855f7;
        font-weight: bold;
    }

    /* 今日の成績 */
    .today-stats {
        background: rgba(16, 185, 129, 0.1);
        border: 1px solid rgba(16, 185, 129, 0.3);
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 30px;
    }

    .stats-title {
        color: #10b981;
        font-weight: 600;
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
        gap: 20px;
    }

    .stat-card {
        text-align: center;
        background: rgba(16, 185, 129, 0.1);
        padding: 15px;
        border-radius: 8px;
    }

    .stat-number {
        color: #10b981;
        font-size: 1.5rem;
        font-weight: 700;
    }

    .stat-desc {
        color: #94a3b8;
        font-size: 0.85rem;
        margin-top: 4px;
    }

    @media (max-width: 768px) {
        .page-title {
            font-size: 2rem;
        }

        .container {
            padding: 20px 15px;
        }

        .race-card {
            padding: 20px;
        }

        .horses-grid {
            grid-template-columns: 1fr;
        }

        .strategies-grid {
            grid-template-columns: 1fr;
        }
    }
</style>

<script type="module">
    // Supabase設定は実際の環境変数に置き換え
    const SUPABASE_URL = 'your-supabase-url';
    const SUPABASE_ANON_KEY = 'your-supabase-anon-key';

    // モックデータ（開発用）
    const mockStandardPredictions = [
        {
            id: 1,
            race_number: 9,
            race_name: "サラ系3歳以上",
            race_date: new Date().toISOString().split('T')[0],
            race_track: "大井",
            race_distance: 1200,
            confidence_score: 87.3,
            prediction_data: {
                horses: [
                    {
                        number: 5,
                        name: "ターフライダー", 
                        mark: "◎",
                        factors: ["先行力: 92.1%", "コース適性: A", "騎手巧者: 89.4%"]
                    },
                    {
                        number: 2,
                        name: "スピードキング",
                        mark: "○", 
                        factors: ["瞬発力: 88.7%", "直線巧者: 85.2%", "調教良好: S"]
                    },
                    {
                        number: 7,
                        name: "ダートファイター",
                        mark: "▲",
                        factors: ["ダート巧者: 91.8%", "距離適性: A", "上がり3F: 86.1%"]
                    }
                ],
                strategies: {
                    safe: {
                        title: "堅実型",
                        hitRate: "81.2",
                        returnRate: "125",
                        recommendation: 4,
                        bets: [
                            { type: "馬連", horses: "5-2", points: "1点" },
                            { type: "馬単", horses: "5→2", points: "1点" }
                        ]
                    },
                    balance: {
                        title: "バランス型", 
                        hitRate: "68.4",
                        returnRate: "162",
                        recommendation: 3,
                        bets: [
                            { type: "馬単", horses: "5→2,7", points: "2点" },
                            { type: "3連複", horses: "5-2-7", points: "1点" }
                        ]
                    }
                }
            }
        },
        {
            id: 2,
            race_number: 10,
            race_name: "サラ系3歳以上",
            race_date: new Date().toISOString().split('T')[0],
            race_track: "大井",
            race_distance: 1600,
            confidence_score: 92.1,
            prediction_data: {
                horses: [
                    {
                        number: 3,
                        name: "アイアンホース",
                        mark: "◎",
                        factors: ["能力指数: 94.3", "安定性: 96.7%", "騎手相性: S"]
                    },
                    {
                        number: 8,
                        name: "ロケットダッシュ",
                        mark: "○",
                        factors: ["逃げ切り力: 87.9%", "ペース適性: A", "枠順有利: 88.2%"]
                    },
                    {
                        number: 1,
                        name: "サイレントキラー", 
                        mark: "▲",
                        factors: ["差し決め手: 91.5%", "直線巧者: 89.8%", "調教抜群: S"]
                    }
                ],
                strategies: {
                    safe: {
                        title: "堅実型",
                        hitRate: "85.7",
                        returnRate: "138",
                        recommendation: 4,
                        bets: [
                            { type: "馬単", horses: "3→8", points: "1点" },
                            { type: "馬連", horses: "3-8", points: "1点" }
                        ]
                    },
                    balance: {
                        title: "バランス型",
                        hitRate: "72.1", 
                        returnRate: "189",
                        recommendation: 3,
                        bets: [
                            { type: "馬単", horses: "3→8,1", points: "2点" },
                            { type: "3連複", horses: "3-8-1", points: "1点" }
                        ]
                    }
                }
            }
        },
        {
            id: 3,
            race_number: 11,
            race_name: "羽田盃(SIII)",
            race_date: new Date().toISOString().split('T')[0],
            race_track: "大井",
            race_distance: 1800,
            confidence_score: 94.8,
            prediction_data: {
                horses: [
                    {
                        number: 6,
                        name: "エンペラーズカップ",
                        mark: "◎",
                        factors: ["格上挑戦: 93.2%", "距離延長プラス: A", "重賞実績: S"]
                    },
                    {
                        number: 4,
                        name: "ゴールドスター",
                        mark: "○",
                        factors: ["重賞常連: 89.7%", "コース巧者: A", "陣営信頼度: 92.1%"]
                    },
                    {
                        number: 9,
                        name: "ダイヤモンドキング",
                        mark: "▲", 
                        factors: ["爆発力: 95.3%", "穴狙い適性: S", "オッズ妙味: 88.9%"]
                    }
                ],
                strategies: {
                    safe: {
                        title: "堅実型",
                        hitRate: "78.9",
                        returnRate: "142", 
                        recommendation: 4,
                        bets: [
                            { type: "馬単", horses: "6→4", points: "1点" },
                            { type: "馬連", horses: "6-4", points: "1点" }
                        ]
                    },
                    balance: {
                        title: "バランス型",
                        hitRate: "65.3",
                        returnRate: "205",
                        recommendation: 3,
                        bets: [
                            { type: "馬単", horses: "6→4,9", points: "2点" },
                            { type: "3連複", horses: "6-4-9", points: "1点" }
                        ]
                    }
                }
            }
        }
    ];

    async function checkStandardAccess() {
        // 開発環境では常にアクセス許可
        // 本番環境ではSupabaseでの認証チェック
        const hasAccess = true; // 実際にはSupabaseでスタンダード会員かチェック
        
        const loadingState = document.getElementById('loading-state');
        const accessDenied = document.getElementById('access-denied');
        const standardContent = document.getElementById('standard-content');
        
        if (hasAccess) {
            loadingState.style.display = 'none';
            standardContent.style.display = 'block';
            await loadStandardPredictions();
        } else {
            loadingState.style.display = 'none';
            accessDenied.style.display = 'block';
        }
    }

    async function loadStandardPredictions() {
        const container = document.getElementById('predictions-container');
        
        try {
            // 本番環境では実際のAPI呼び出し
            // const predictions = await fetchStandardPredictions();
            
            // 開発環境ではモックデータを使用
            const predictions = mockStandardPredictions;
            
            if (predictions.length === 0) {
                container.innerHTML = `
                    <div class="no-data">
                        <div class="no-data-icon">📅</div>
                        <div class="no-data-title">本日の予想はまだ準備中です</div>
                        <div class="no-data-text">
                            スタンダード予想は開催日の前日夜に公開されます。<br>
                            後半3レース（9R-11R）の精密な予想をお届けします。
                        </div>
                    </div>
                `;
                return;
            }

            container.innerHTML = predictions.map(prediction => `
                <div class="race-card">
                    <!-- レースヘッダー -->
                    <div class="race-header-section">
                        <div class="ai-badge">
                            <span class="pulse"></span>
                            <span class="ai-status">AI分析完了</span>
                        </div>
                        <h1 class="race-title">${prediction.race_track}${prediction.race_number}R ${prediction.race_name}</h1>
                        <div class="ai-metrics">
                            <div class="metric">
                                <div class="metric-value">${prediction.confidence_score}%</div>
                                <div class="metric-label">予測信頼度</div>
                            </div>
                            <div class="metric">
                                <div class="metric-value">85.2</div>
                                <div class="metric-label">能力指数</div>
                            </div>
                            <div class="metric">
                                <div class="metric-value">A+</div>
                                <div class="metric-label">推奨度</div>
                            </div>
                            <div class="metric">
                                <div class="metric-value">165%</div>
                                <div class="metric-label">期待回収率</div>
                            </div>
                        </div>
                    </div>

                    <!-- AI選出馬分析 -->
                    <div class="analysis-section">
                        <h2 class="section-title">
                            <span>🤖</span>
                            <span>AIモデル選出馬 - スタンダード予想</span>
                        </h2>
                        
                        ${prediction.prediction_data.horses.map(horse => {
                            const markClass = horse.mark === '◎' ? 'main' : 'sub';
                            const typeText = horse.mark === '◎' ? '本命' : horse.mark === '○' ? '対抗' : '単穴';
                            const typeColor = horse.mark === '◎' ? '#10b981' : horse.mark === '○' ? '#3b82f6' : '#f59e0b';
                            
                            return `
                                <div class="horse-card horse-card-${markClass}">
                                    <div class="horse-header">
                                        <div>
                                            <span class="horse-mark-${markClass}">${horse.mark}</span>
                                            <span class="horse-number">${horse.number}</span>
                                            <span class="horse-name">${horse.name}</span>
                                            <span class="horse-type" style="color: ${typeColor};">${typeText}</span>
                                        </div>
                                        <span class="confidence-score" style="color: ${typeColor};">信頼度 85%</span>
                                    </div>
                                    <div class="ai-factors">
                                        ${horse.factors.map(factor => `
                                            <div class="factor">
                                                <span class="factor-icon">✓</span>
                                                <span>${factor}</span>
                                            </div>
                                        `).join('')}
                                    </div>
                                </div>
                            `;
                        }).join('')}
                    </div>

                    <!-- 投資戦略 -->
                    <div class="recommendation-section">
                        <h2 class="section-title">
                            <span>💰</span>
                            <span>投資戦略（スタンダード限定）</span>
                        </h2>
                        
                        ${Object.entries(prediction.prediction_data.strategies).map(([key, strategy]) => {
                            const riskLevel = key === 'safe' ? 'low' : 'medium';
                            const riskBars = key === 'safe' ? 1 : 2;
                            
                            return `
                                <div class="bet-strategy">
                                    <div class="strategy-title">🎯 ${strategy.title}（推奨度 ${'★'.repeat(strategy.recommendation)}${'☆'.repeat(5-strategy.recommendation)}）</div>
                                    <div style="color: #94a3b8; margin-bottom: 15px;">
                                        AIモデル予測: 的中率${strategy.hitRate}% / 期待回収率${strategy.returnRate}%
                                    </div>
                                    <div class="bet-list">
                                        ${strategy.bets.map(bet => `
                                            <div class="bet-item">
                                                <span class="bet-type">${bet.type}</span>
                                                <span class="bet-horses">${bet.horses}</span>
                                                <span class="bet-points">${bet.points}</span>
                                            </div>
                                        `).join('')}
                                    </div>
                                    <div class="risk-indicator">
                                        <span style="color: #64748b; font-size: 0.85rem;">リスク:</span>
                                        ${[1,2,3].map(i => `
                                            <div class="risk-bar ${i <= riskBars ? `active risk-${riskLevel}` : ''}"></div>
                                        `).join('')}
                                    </div>
                                    <div class="expected-return">
                                        <div class="return-value">推定配当 5-8倍</div>
                                        <div style="color: #94a3b8; font-size: 0.9rem;">堅実決着想定</div>
                                    </div>
                                </div>
                            `;
                        }).join('')}
                    </div>
                </div>
            `).join('');

        } catch (error) {
            console.error('予想データの読み込みエラー:', error);
            container.innerHTML = `
                <div class="no-data">
                    <div class="no-data-icon">❌</div>
                    <div class="no-data-title">予想データの読み込みに失敗しました</div>
                    <div class="no-data-text">
                        しばらく時間をおいてから再度お試しください。
                    </div>
                </div>
            `;
        }
    }

    // ページ読み込み時に実行
    document.addEventListener('DOMContentLoaded', checkStandardAccess);
</script>