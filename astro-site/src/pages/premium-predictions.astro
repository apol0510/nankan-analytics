---
// 有料予想ページ（会員限定）
---

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>プレミアム予想 | NANKANアナリティクス</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #e2e8f0;
            min-height: 100vh;
        }

        .navbar {
            background: rgba(30, 41, 59, 0.9);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(59, 130, 246, 0.3);
            padding: 16px 0;
        }

        .navbar-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }

        .logo {
            color: #3b82f6;
            font-size: 20px;
            font-weight: 700;
            text-decoration: none;
        }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .premium-badge {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
            color: white;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 600;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .page-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .page-title {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 12px;
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .page-subtitle {
            color: #94a3b8;
            font-size: 18px;
        }

        .loading-state {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 400px;
            color: #94a3b8;
        }

        .spinner {
            width: 32px;
            height: 32px;
            border: 3px solid #475569;
            border-top: 3px solid #3b82f6;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-right: 16px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .predictions-grid {
            display: grid;
            gap: 24px;
        }

        .prediction-card {
            background: rgba(30, 41, 59, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 12px;
            padding: 24px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .race-header {
            display: flex;
            justify-content: between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 12px;
        }

        .race-title {
            font-size: 20px;
            font-weight: 600;
            color: #f1f5f9;
        }

        .race-meta {
            color: #94a3b8;
            font-size: 14px;
        }

        .confidence-score {
            background: rgba(34, 197, 94, 0.1);
            color: #86efac;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 500;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }

        .horses-section {
            margin-bottom: 24px;
        }

        .horses-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 16px;
        }

        .horse-card {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(59, 130, 246, 0.2);
            border-radius: 8px;
            padding: 16px;
        }

        .horse-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 12px;
        }

        .horse-name {
            font-weight: 600;
            color: #f1f5f9;
        }

        .horse-number {
            background: #3b82f6;
            color: white;
            width: 24px;
            height: 24px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            font-weight: 600;
        }

        .horse-mark {
            font-size: 18px;
            margin-left: 8px;
        }

        .horse-factors {
            list-style: none;
        }

        .horse-factors li {
            display: flex;
            align-items: center;
            font-size: 13px;
            color: #cbd5e1;
            margin-bottom: 4px;
        }

        .factor-icon {
            margin-right: 8px;
            font-size: 12px;
        }

        .strategies-section {
            margin-top: 24px;
        }

        .strategies-title {
            font-size: 18px;
            font-weight: 600;
            color: #f1f5f9;
            margin-bottom: 16px;
        }

        .strategy-tabs {
            display: flex;
            gap: 8px;
            margin-bottom: 16px;
        }

        .strategy-tab {
            padding: 8px 16px;
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 6px;
            color: #94a3b8;
            cursor: pointer;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .strategy-tab.active {
            background: rgba(59, 130, 246, 0.2);
            color: #3b82f6;
            border-color: #3b82f6;
        }

        .strategy-content {
            background: rgba(15, 23, 42, 0.4);
            border-radius: 8px;
            padding: 16px;
        }

        .strategy-meta {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            font-size: 14px;
        }

        .hit-rate {
            color: #10b981;
        }

        .return-rate {
            color: #f59e0b;
        }

        .bet-list {
            list-style: none;
        }

        .bet-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid rgba(59, 130, 246, 0.1);
        }

        .bet-item:last-child {
            border-bottom: none;
        }

        .bet-type {
            font-weight: 500;
            color: #f1f5f9;
        }

        .bet-horses {
            color: #94a3b8;
            font-size: 14px;
        }

        .bet-points {
            color: #64748b;
            font-size: 12px;
        }

        .access-denied {
            text-align: center;
            padding: 60px 20px;
        }

        .access-denied-icon {
            font-size: 64px;
            margin-bottom: 24px;
        }

        .access-denied-title {
            font-size: 24px;
            font-weight: 700;
            color: #f1f5f9;
            margin-bottom: 16px;
        }

        .access-denied-message {
            color: #94a3b8;
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 32px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }

        .btn {
            display: inline-block;
            padding: 12px 24px;
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px -5px rgba(59, 130, 246, 0.4);
        }

        @media (max-width: 768px) {
            .horses-grid {
                grid-template-columns: 1fr;
            }
            
            .strategy-tabs {
                flex-wrap: wrap;
            }
            
            .container {
                padding: 20px 16px;
            }
        }
    </style>
</head>
<body>
    <!-- ナビゲーションバー -->
    <nav class="navbar">
        <div class="navbar-content">
            <a href="/" class="logo">🤖 NANKANアナリティクス</a>
            <div class="nav-right">
                <span class="premium-badge">PREMIUM</span>
                <span id="user-email">ロード中...</span>
                <a href="/dashboard" style="color: #94a3b8; text-decoration: none;">ダッシュボード</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="page-header">
            <h1 class="page-title">🔥 プレミアム予想</h1>
            <p class="page-subtitle">AI分析による高精度予想・詳細戦略</p>
        </div>

        <!-- ローディング状態 -->
        <div id="loading-state" class="loading-state">
            <div class="spinner"></div>
            認証情報を確認中...
        </div>

        <!-- アクセス拒否 -->
        <div id="access-denied" class="access-denied" style="display: none;">
            <div class="access-denied-icon">🔒</div>
            <h2 class="access-denied-title">プレミアム会員限定コンテンツ</h2>
            <p class="access-denied-message">
                このコンテンツをご覧いただくには、プレミアム会員への登録が必要です。<br>
                AIによる高精度予想と詳細な分析レポートをお楽しみください。
            </p>
            <a href="/pricing" class="btn">プレミアム会員になる</a>
        </div>

        <!-- プレミアムコンテンツ -->
        <div id="premium-content" style="display: none;">
            <div class="predictions-grid" id="predictions-container">
                <!-- 予想データがここに表示される -->
            </div>
        </div>
    </div>

    <script type="module">
        import { auth, profiles } from '/src/lib/supabase.js';
        import { supabase } from '/src/lib/supabase.js';

        let currentUser = null;

        // 初期化
        document.addEventListener('DOMContentLoaded', async () => {
            await checkAuthAndAccess();
        });

        // 認証とアクセス権チェック
        async function checkAuthAndAccess() {
            const loadingState = document.getElementById('loading-state');
            const accessDenied = document.getElementById('access-denied');
            const premiumContent = document.getElementById('premium-content');

            try {
                // 認証チェック
                const { data: { user }, error } = await auth.getUser();
                
                if (error || !user) {
                    // 未認証の場合はログインページへ
                    window.location.href = '/auth/login?redirect=' + encodeURIComponent(window.location.pathname);
                    return;
                }
                
                currentUser = user;
                document.getElementById('user-email').textContent = user.email;

                // プロフィール情報取得
                const { data: profile, error: profileError } = await profiles.getProfile(user.id);
                
                if (profileError) {
                    throw profileError;
                }

                // アクセス権チェック
                const hasAccess = profile?.subscription_status === 'active' && 
                                 (profile?.subscription_plan === 'standard' || profile?.subscription_plan === 'premium');

                if (!hasAccess) {
                    // アクセス権なし
                    loadingState.style.display = 'none';
                    accessDenied.style.display = 'block';
                    return;
                }

                // アクセス権あり - コンテンツ表示
                loadingState.style.display = 'none';
                premiumContent.style.display = 'block';
                
                await loadPremiumPredictions();

            } catch (error) {
                console.error('Access check error:', error);
                loadingState.style.display = 'none';
                accessDenied.style.display = 'block';
            }
        }

        // プレミアム予想データ読み込み
        async function loadPremiumPredictions() {
            const container = document.getElementById('predictions-container');
            
            try {
                // Supabaseから予想データを取得
                const { data: predictions, error } = await supabase
                    .from('prediction_content')
                    .select('*')
                    .eq('content_type', 'premium')
                    .eq('is_published', true)
                    .gte('race_date', new Date().toISOString().split('T')[0])
                    .order('race_date', { ascending: true })
                    .limit(10);

                if (error) {
                    throw error;
                }

                if (predictions && predictions.length > 0) {
                    // 実データを表示
                    container.innerHTML = predictions.map(prediction => 
                        renderPredictionCard(prediction.prediction_data)
                    ).join('');
                } else {
                    // デモデータを表示
                    container.innerHTML = renderDemoData();
                }

            } catch (error) {
                console.error('Predictions loading error:', error);
                // エラー時もデモデータを表示
                container.innerHTML = renderDemoData();
            }
        }

        // デモデータレンダリング
        function renderDemoData() {
            return `
                <div class="prediction-card">
                    <div class="race-header">
                        <div>
                            <div class="race-title">大井11R 羽田盃</div>
                            <div class="race-meta">2025年8月23日 | ダート1600m | 16頭立て</div>
                        </div>
                        <div class="confidence-score">信頼度: 91.2%</div>
                    </div>

                    <div class="horses-section">
                        <div class="horses-grid">
                            <div class="horse-card">
                                <div class="horse-header">
                                    <div>
                                        <span class="horse-name">アンジュルナ</span>
                                        <span class="horse-mark">◎</span>
                                    </div>
                                    <div class="horse-number">8</div>
                                </div>
                                <ul class="horse-factors">
                                    <li><span class="factor-icon">◎</span>能力指数: 86.8 (1位)</li>
                                    <li><span class="factor-icon">◎</span>安定性スコア: 91.9%</li>
                                    <li><span class="factor-icon">◎</span>展開適性: S評価</li>
                                </ul>
                            </div>

                            <div class="horse-card">
                                <div class="horse-header">
                                    <div>
                                        <span class="horse-name">アムールピスケス</span>
                                        <span class="horse-mark">○</span>
                                    </div>
                                    <div class="horse-number">1</div>
                                </div>
                                <ul class="horse-factors">
                                    <li><span class="factor-icon">○</span>スピード指数: 76.2</li>
                                    <li><span class="factor-icon">○</span>先行力: 89.3%</li>
                                    <li><span class="factor-icon">○</span>枠順優位性: A級</li>
                                </ul>
                            </div>

                            <div class="horse-card">
                                <div class="horse-header">
                                    <div>
                                        <span class="horse-name">ロードレイジング</span>
                                        <span class="horse-mark">▲</span>
                                    </div>
                                    <div class="horse-number">5</div>
                                </div>
                                <ul class="horse-factors">
                                    <li><span class="factor-icon">▲</span>爆発力指数: 89.6</li>
                                    <li><span class="factor-icon">▲</span>ムラ係数: 高変動</li>
                                    <li><span class="factor-icon">▲</span>展開次第: 要注意</li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <div class="strategies-section">
                        <div class="strategies-title">投資戦略</div>
                        <div class="strategy-tabs">
                            <div class="strategy-tab active" onclick="showStrategy(this, 'safe')">高的中率型</div>
                            <div class="strategy-tab" onclick="showStrategy(this, 'balance')">バランス型</div>
                            <div class="strategy-tab" onclick="showStrategy(this, 'aggressive')">高配当型</div>
                        </div>
                        
                        <div class="strategy-content" id="strategy-safe">
                            <div class="strategy-meta">
                                <span class="hit-rate">的中率: 78.5%</span>
                                <span class="return-rate">回収率: 132%</span>
                            </div>
                            <ul class="bet-list">
                                <li class="bet-item">
                                    <div>
                                        <div class="bet-type">馬連</div>
                                        <div class="bet-horses">8 - 1,5</div>
                                    </div>
                                    <div class="bet-points">2点</div>
                                </li>
                                <li class="bet-item">
                                    <div>
                                        <div class="bet-type">馬単</div>
                                        <div class="bet-horses">8 → 1,5</div>
                                    </div>
                                    <div class="bet-points">2点</div>
                                </li>
                                <li class="bet-item">
                                    <div>
                                        <div class="bet-type">3連複</div>
                                        <div class="bet-horses">8-1,5 + 9,2,3</div>
                                    </div>
                                    <div class="bet-points">6点</div>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            `;
        }

        // 予想カードレンダリング
        function renderPredictionCard(predictionData) {
            // 実際のデータ構造に応じてレンダリング
            return renderDemoData(); // 暫定的にデモデータを返す
        }

        // 戦略タブ切り替え
        window.showStrategy = function(tab, strategyType) {
            // タブの状態更新
            document.querySelectorAll('.strategy-tab').forEach(t => t.classList.remove('active'));
            tab.classList.add('active');

            // 戦略コンテンツの更新（簡略化）
            console.log('Strategy switched to:', strategyType);
        }
    </script>
</body>
</html>