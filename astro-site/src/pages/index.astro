---
// 静的ビルドのためprerender削除
import BaseLayout from '../layouts/BaseLayout.astro';
import raceResults from '../data/raceResults.json';
import allRacesData from '../data/allRacesPrediction.json';
import { yesterdayResults, weeklyResults, dailyTabs, dailyResultsData } from './standard-predictions.astro';

// データ取得
const currentResults = raceResults; // raceResults.json contains the current results directly
// メインレース（11R）のデータを取得 - 防御的プログラミング実装
const mainRace = (allRacesData && Array.isArray(allRacesData.races))
    ? allRacesData.races.find(race => race.isMainRace === true)
    : null;

if (!mainRace) {
    console.error('❌ メインレースデータが見つかりません:', { allRacesData: allRacesData ? 'exists' : 'missing' });
    throw new Error('メインレースデータが見つかりません');
}

const { raceInfo, strategies, horses } = mainRace;

// 戦略データから信頼度を計算
function calculateAverageConfidence(strategies) {
    if (!strategies) return 0;

    const strategyKeys = ['safe', 'balance', 'aggressive'];
    const confidenceValues = strategyKeys
        .map(key => strategies[key]?.confidence || strategies[key]?.hitRate)
        .filter(value => value !== undefined && value !== null)
        .map(value => parseInt(value));

    if (confidenceValues.length === 0) return 0;

    const average = confidenceValues.reduce((sum, val) => sum + val, 0) / confidenceValues.length;
    return Math.round(average);
}

// プレビューコンテンツを生成
function generatePreview(raceInfo, strategies, horses) {
    if (!raceInfo || !strategies) {
        return {
            summary: 'AI予想データの準備中です',
            features: ''
        };
    }

    // 本命の累積スコアを信頼度として使用
    let confidence = calculateAverageConfidence(strategies);
    if (horses?.main?.factors) {
        const scoreText = horses.main.factors.find(factor => factor.text && factor.text.includes('累積スコア'));
        if (scoreText) {
            const scoreMatch = scoreText.text.match(/(\d+)pt/);
            if (scoreMatch) {
                confidence = parseInt(scoreMatch[1]);
            }
        }
    }

    const safeTitle = strategies.safe?.title || '少点数的中型';
    const balanceTitle = strategies.balance?.title || 'バランス型';
    const aggressiveTitle = strategies.aggressive?.title || '高配当追求型';

    // 本命・対抗の馬名を取得
    const mainHorseName = horses?.main?.name || '';
    const subHorseName = horses?.sub?.name || '';
    const horseInfo = mainHorseName && subHorseName ? `本命${mainHorseName}・対抗${subHorseName}` : '';

    const summary = `${raceInfo.track} ${raceInfo.raceNumber} AI予想完了！`;
    const features = horseInfo ? `${horseInfo}\n信頼度${confidence}% | ${safeTitle}・${balanceTitle}・${aggressiveTitle}で的中と配当のバランスを最適化。` : `信頼度${confidence}% | ${safeTitle}・${balanceTitle}・${aggressiveTitle}で的中と配当のバランスを最適化。`;

    return { summary, features };
}

// 信頼度計算とプレビュー生成（本命の累積スコアを使用）
let calculatedConfidence = calculateAverageConfidence(strategies);
if (horses?.main?.factors) {
    const scoreText = horses.main.factors.find(factor => factor.text && factor.text.includes('累積スコア'));
    if (scoreText) {
        const scoreMatch = scoreText.text.match(/(\d+)pt/);
        if (scoreMatch) {
            calculatedConfidence = parseInt(scoreMatch[1]);
        }
    }
}
const preview = generatePreview(raceInfo, strategies, horses);

// raceInfoに計算した信頼度を追加
const enhancedRaceInfo = {
    ...raceInfo,
    confidence: calculatedConfidence
};

// 日付フォーマット変換
function formatDate(dateStr) {
    const date = new Date(dateStr);
    const month = date.getMonth() + 1;
    const day = date.getDate();
    return `${month}月${day}日`;
}

// レース情報の整形
const formattedDate = enhancedRaceInfo?.date ? formatDate(enhancedRaceInfo.date) : '日付未設定';
const raceTitle = `${formattedDate} ${enhancedRaceInfo?.track !== 'レース情報未検出' && enhancedRaceInfo?.track ? enhancedRaceInfo.track : ''} ${enhancedRaceInfo?.raceNumber || ''}${enhancedRaceInfo?.raceName !== 'レース情報未検出' && enhancedRaceInfo?.raceName ? ' ' + enhancedRaceInfo.raceName : ''}`;
---

<BaseLayout title="NANKANアナリティクス | AI競馬予想で投資効率を最大化">
            <!-- Hero Section -->
            <section class="hero-section">
                <h1 class="hero-title">AI競馬予想で投資効率を最大化</h1>
                <p class="hero-subtitle">
                    XGBoost・LSTM・ニューラルネットワークによる高精度分析で<br>
                    的中率87%、回収率156%を実現
                </p>
                <p class="hero-description">
                    従来の感覚的予想から、データサイエンスによる科学的投資へ。<br>
                    AIが24時間365日、南関競馬を分析し続けています。
                </p>

                <!-- AI Statistics Cards -->
                <div class="hero-stats-section" style="margin-top: 50px;">
                    <div class="stats-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; max-width: 100%; margin: 0 auto; padding: 0 10px;">
                        <!-- AI Model Accuracy -->
                        <div class="stats-card" style="background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(139, 92, 246, 0.15) 100%); padding: 20px 15px; border-radius: 10px; text-align: center; border: 2px solid rgba(59, 130, 246, 0.2); transition: transform 0.3s, box-shadow 0.3s;">
                            <div class="stats-number" data-target="87.3" data-suffix="%" style="font-size: 2.2rem; font-weight: 800; color: var(--success-color); margin-bottom: 8px; text-shadow: 0 2px 4px rgba(16, 185, 129, 0.3);">
                                0%
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600; font-size: 0.95rem; margin-bottom: 5px;">
                                AIモデル精度
                            </div>
                            <div style="color: var(--accent-text-color); font-size: 0.75rem; line-height: 1.3;">
                                XGBoost・LSTM技術
                            </div>
                        </div>

                        <!-- Average Return Rate -->
                        <div class="stats-card" style="background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(59, 130, 246, 0.15) 100%); padding: 20px 15px; border-radius: 10px; text-align: center; border: 2px solid rgba(16, 185, 129, 0.2); transition: transform 0.3s, box-shadow 0.3s;">
                            <div class="stats-number" data-target="156" data-suffix="%" style="font-size: 2.2rem; font-weight: 800; color: var(--success-color); margin-bottom: 8px; text-shadow: 0 2px 4px rgba(16, 185, 129, 0.3);">
                                0%
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600; font-size: 0.95rem; margin-bottom: 5px;">
                                平均回収率
                            </div>
                            <div style="color: var(--accent-text-color); font-size: 0.75rem; line-height: 1.3;">
                                投資効率最大化
                            </div>
                        </div>

                        <!-- Training Data -->
                        <div class="stats-card" style="background: linear-gradient(135deg, rgba(139, 92, 246, 0.15) 0%, rgba(59, 130, 246, 0.15) 100%); padding: 20px 15px; border-radius: 10px; text-align: center; border: 2px solid rgba(139, 92, 246, 0.2); transition: transform 0.3s, box-shadow 0.3s;">
                            <div class="stats-number" data-target="10000" data-suffix="+" style="font-size: 2.2rem; font-weight: 800; color: var(--success-color); margin-bottom: 8px; text-shadow: 0 2px 4px rgba(16, 185, 129, 0.3);">
                                0+
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600; font-size: 0.95rem; margin-bottom: 5px;">
                                学習データ数
                            </div>
                            <div style="color: var(--accent-text-color); font-size: 0.75rem; line-height: 1.3;">
                                5年間レースデータ
                            </div>
                        </div>

                        <!-- Features -->
                        <div class="stats-card" style="background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(139, 92, 246, 0.15) 100%); padding: 20px 15px; border-radius: 10px; text-align: center; border: 2px solid rgba(16, 185, 129, 0.2); transition: transform 0.3s, box-shadow 0.3s;">
                            <div class="stats-number" data-target="50" data-suffix="+" style="font-size: 2.2rem; font-weight: 800; color: var(--success-color); margin-bottom: 8px; text-shadow: 0 2px 4px rgba(16, 185, 129, 0.3);">
                                0+
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600; font-size: 0.95rem; margin-bottom: 5px;">
                                特徴量
                            </div>
                            <div style="color: var(--accent-text-color); font-size: 0.75rem; line-height: 1.3;">
                                多角度分析ファクター
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- 的中実績セクション -->
            <section style="padding: 60px 20px; background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);">
                <div class="container" style="max-width: 1200px; margin: 0 auto;">
                    <div style="text-align: center; margin-bottom: 40px;">
                        <h2 style="font-size: 2.5rem; font-weight: 800; background: linear-gradient(135deg, #10b981 0%, #3b82f6 50%, #8b5cf6 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; margin-bottom: 15px;">
                            🏆 昨日の的中結果
                        </h2>
                        <p style="color: white; font-size: 1.1rem;">
                            9/16 大井競馬での予想成績
                        </p>
                    </div>
                    
                    <!-- 的中サマリー -->
                    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-bottom: 40px;">
                        <div style="background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(16, 185, 129, 0.1) 100%); border: 2px solid rgba(16, 185, 129, 0.3); border-radius: 16px; padding: 25px; text-align: center;">
                            <div style="font-size: 3rem; font-weight: 800; color: #10b981; margin-bottom: 10px;">
                                {currentResults?.hits || 0}/{currentResults?.totalRaces || 0}
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600;">
                                的中率 {currentResults?.hitRate || '0%'}
                            </div>
                        </div>
                        
                        <div style="background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(139, 92, 246, 0.1) 100%); border: 2px solid rgba(59, 130, 246, 0.3); border-radius: 16px; padding: 25px; text-align: center;">
                            <div style="font-size: 3rem; font-weight: 800; color: #3b82f6; margin-bottom: 10px;">
                                ¥{currentResults?.payout?.toLocaleString() || '0'}
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600;">
                                総配当金額
                            </div>
                        </div>
                        
                        <div style="background: linear-gradient(135deg, rgba(139, 92, 246, 0.15) 0%, rgba(236, 72, 153, 0.1) 100%); border: 2px solid rgba(139, 92, 246, 0.3); border-radius: 16px; padding: 25px; text-align: center;">
                            <div style="font-size: 3rem; font-weight: 800; color: #8b5cf6; margin-bottom: 10px;">
                                {currentResults?.returnRate || '0%'}
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600;">
                                回収率
                            </div>
                        </div>
                    </div>
                    
                    <!-- コンパクトな的中一覧 -->
                    <div style="background: rgba(255, 255, 255, 0.03); border-radius: 16px; padding: 30px; border: 1px solid rgba(148, 163, 184, 0.1);">
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(100px, 1fr)); gap: 12px; max-width: 1000px; margin: 0 auto;">
                            {currentResults?.races?.map((race) => (
                                <div style={`display: flex; flex-direction: column; align-items: center; padding: 15px 10px; ${race.status === 'hit' 
                                    ? 'background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.1) 100%); border: 1px solid rgba(16, 185, 129, 0.3);' 
                                    : 'background: rgba(239, 68, 68, 0.1); border: 1px solid rgba(239, 68, 68, 0.2);'} border-radius: 12px;`}>
                                    <span style="color: var(--body-text-color); font-weight: 600; font-size: 0.9rem; margin-bottom: 8px;">{race.race}</span>
                                    <span style={`font-size: 1.3rem; ${race.status === 'hit' ? 'color: #10b981;' : 'color: #ef4444;'}`}>
                                        {race.status === 'hit' ? '⭕' : '❌'}
                                    </span>
                                    {race.status === 'hit' ? (
                                        <>
                                            <span style="color: #10b981; font-weight: 700; font-size: 0.75rem; opacity: 0.8;">{race.betType}</span>
                                            <span style="color: #10b981; font-weight: 700; font-size: 0.85rem; margin-top: 2px;">¥{race.payout.toLocaleString()}</span>
                                        </>
                                    ) : (
                                        <span style="color: #6b7280; font-size: 0.85rem; margin-top: 5px;">-</span>
                                    )}
                                </div>
                            ))}
                        </div>
                    </div>
                </div>
            </section>

            <!-- Last Week's Results -->
            <section style="margin-bottom: 60px;">
                <div class="prediction-container">
                    <h2 style="font-size: 2.5rem; font-weight: 800; color: var(--primary-color); text-align: center; margin-bottom: 15px;">
                        🏆 先週の的中結果
                    </h2>
                    <p style="text-align: center; color: var(--body-text-color); font-size: 1.1rem; margin-bottom: 40px; opacity: 0.9;">
                        {weeklyResults.period} {weeklyResults.track}での予想成績
                    </p>

                    <!-- 週間サマリー統計 -->
                    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 40px; max-width: 900px; margin-left: auto; margin-right: auto;">
                        <div style="background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(34, 197, 94, 0.1) 100%); border: 2px solid rgba(16, 185, 129, 0.3); border-radius: 16px; padding: 25px; text-align: center;">
                            <div style="font-size: 3rem; font-weight: 800; color: #10b981; margin-bottom: 10px;">
                                {weeklyResults.hitRate}%
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600;">
                                的中率 ({weeklyResults.hitCount}/{weeklyResults.totalCount})
                            </div>
                        </div>

                        <div style="background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(99, 102, 241, 0.1) 100%); border: 2px solid rgba(59, 130, 246, 0.3); border-radius: 16px; padding: 25px; text-align: center;">
                            <div style="font-size: 3rem; font-weight: 800; color: #3b82f6; margin-bottom: 10px;">
                                ¥{weeklyResults.totalPayout.toLocaleString()}
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600;">
                                週間配当
                            </div>
                        </div>

                        <div style="background: linear-gradient(135deg, rgba(139, 92, 246, 0.15) 0%, rgba(236, 72, 153, 0.1) 100%); border: 2px solid rgba(139, 92, 246, 0.3); border-radius: 16px; padding: 25px; text-align: center;">
                            <div style="font-size: 3rem; font-weight: 800; color: #8b5cf6; margin-bottom: 10px;">
                                {weeklyResults.recoveryRate}%
                            </div>
                            <div style="color: var(--body-text-color); font-weight: 600;">
                                週間回収率
                            </div>
                        </div>
                    </div>
                    
                    <!-- 日別タブ -->
                    <div style="background: rgba(255, 255, 255, 0.03); border-radius: 16px; padding: 30px; border: 1px solid rgba(148, 163, 184, 0.1);">
                        <div style="display: flex; justify-content: center; margin-bottom: 30px; flex-wrap: wrap; gap: 10px;">
                            {dailyTabs.map((tab, index) => (
                                <button
                                    onclick={`showWeeklyResults('${tab.date}')`}
                                    id={`tab-${tab.date}`}
                                    class={`weekly-tab ${index === 0 ? 'active' : ''}`}
                                    style={index === 0
                                        ? "background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.3) 100%); color: #10b981; padding: 12px 24px; border-radius: 25px; font-weight: 600; border: 1px solid rgba(16, 185, 129, 0.3); cursor: pointer;"
                                        : "background: rgba(148, 163, 184, 0.1); color: var(--body-text-color); padding: 12px 24px; border-radius: 25px; font-weight: 600; border: 1px solid rgba(148, 163, 184, 0.2); cursor: pointer;"
                                    }
                                >
                                    {tab.label}
                                </button>
                            ))}
                        </div>

                        <!-- 動的日別結果表示 -->
                        {dailyTabs.map((tab, index) => (
                            <div
                                id={`results-${tab.date}`}
                                class="weekly-results"
                                style={index === 0
                                    ? "display: grid; grid-template-columns: repeat(auto-fit, minmax(100px, 1fr)); gap: 12px; max-width: 1000px; margin: 0 auto;"
                                    : "display: none; grid-template-columns: repeat(auto-fit, minmax(100px, 1fr)); gap: 12px; max-width: 1000px; margin: 0 auto;"
                                }
                            >
                                {dailyResultsData[tab.date].map((result) => (
                                    <div style={result.result === 'win'
                                        ? "display: flex; flex-direction: column; align-items: center; padding: 15px 10px; background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.1) 100%); border-radius: 12px; border: 1px solid rgba(16, 185, 129, 0.3);"
                                        : "display: flex; flex-direction: column; align-items: center; padding: 15px 10px; background: rgba(239, 68, 68, 0.1); border-radius: 12px; border: 1px solid rgba(239, 68, 68, 0.2);"
                                    }>
                                        <span style="color: var(--body-text-color); font-weight: 600; font-size: 0.9rem; margin-bottom: 8px;">{result.race}</span>
                                        <span style={result.result === 'win' ? "color: #10b981; font-size: 1.3rem;" : "color: #ef4444; font-size: 1.3rem;"}>
                                            {result.result === 'win' ? '⭕' : '❌'}
                                        </span>
                                        {result.result === 'win' ? (
                                            <>
                                                <span style="color: #10b981; font-weight: 700; font-size: 0.75rem; opacity: 0.8;">馬単</span>
                                                <span style="color: #10b981; font-weight: 700; font-size: 0.85rem; margin-top: 2px;">¥{result.payout.toLocaleString()}</span>
                                            </>
                                        ) : (
                                            <span style="color: #6b7280; font-size: 0.85rem; margin-top: 5px;">-</span>
                                        )}
                                    </div>
                                ))}
                            </div>
                        ))}


                        <!-- JavaScript for tab functionality -->
                        <script is:inline>
                        function showWeeklyResults(date) {
                            // Hide all result divs
                            const allResults = document.querySelectorAll('.weekly-results');
                            allResults.forEach(result => result.style.display = 'none');
                            
                            // Remove active class from all tabs
                            const allTabs = document.querySelectorAll('.weekly-tab');
                            allTabs.forEach(tab => {
                                tab.className = 'weekly-tab';
                                tab.style.background = 'rgba(148, 163, 184, 0.1)';
                                tab.style.color = 'var(--body-text-color)';
                                tab.style.border = '1px solid rgba(148, 163, 184, 0.2)';
                            });
                            
                            // Show selected result div
                            const selectedResult = document.getElementById('results-' + date);
                            if (selectedResult) {
                                selectedResult.style.display = 'grid';
                            }
                            
                            // Add active class to selected tab
                            const selectedTab = document.getElementById('tab-' + date);
                            if (selectedTab) {
                                selectedTab.className = 'weekly-tab active';
                                selectedTab.style.background = 'linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.3) 100%)';
                                selectedTab.style.color = '#10b981';
                                selectedTab.style.border = '1px solid rgba(16, 185, 129, 0.3)';
                            }
                        }
                        </script>

                        <!-- Hidden tabs for other days (will add JavaScript later) -->
                    </div>
                </div>
            </section>

            <!-- Live Predictions -->
            <section class="live-prediction-section" id="predictions">
                <div style="text-align: center; margin-bottom: 20px;">
                    <div style="display: inline-flex; align-items: center; gap: 12px; background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.3) 100%); padding: 8px 24px; border-radius: 50px; border: 1px solid rgba(16, 185, 129, 0.3);">
                        <span style="width: 8px; height: 8px; background: #ef4444; border-radius: 50%; animation: pulse 2s infinite;"></span>
                        <span style="color: #10b981; font-weight: 600; font-size: 0.9rem;">LIVE 稼働中</span>
                    </div>
                </div>
                <h2 class="section-title">本日のAI予想（プレビュー）</h2>
                <p class="section-subtitle">
                    AIがリアルタイムで分析中。実際の予想プロセスと判断根拠を一部公開。詳細な買い目は有料版で提供中。
                </p>
                <div class="prediction-cards">
                    <div class="prediction-card">
                        <div class="race-info">
                            <span class="race-title">{raceTitle}</span>
                            <span class="confidence-score">信頼度 {Math.floor(parseFloat(enhancedRaceInfo?.confidence || '0'))}%</span>
                        </div>
                        <div class="prediction-summary">
                            {preview?.summary || 'AI予想データの準備中です'}
                            <br><br>
                            {preview?.features || ''}
                            <br><br>
                            リスク分析: {preview?.riskAnalysis || '分析中'}
                        </div>
                        <div class="blur-overlay">
                            <span class="premium-badge"><a href="/free-prediction/" style="color: inherit; text-decoration: underline;">無料予想はこちら</a></span>
                        </div>
                    </div>
                </div>
            </section>


            <!-- AI Technology Section -->
            <section class="tech-section" id="technology">
                <h2 class="section-title">🤖 NANKANアナリティクスの AI技術</h2>
                <p class="section-subtitle">
                    最新の機械学習アルゴリズムを組み合わせ、人間を超える予想精度を実現
                </p>
                <div class="tech-grid">
                    <div class="tech-card">
                        <div class="tech-name">XGBoost勾配ブースティング</div>
                        <div class="tech-accuracy">精度 91.2%</div>
                        <div class="tech-description">
                            150以上の特徴量から最適な組み合わせを自動学習。過去5年間50,000レースのデータで訓練済み。
                        </div>
                    </div>
                    <div class="tech-card">
                        <div class="tech-name">LSTM時系列分析</div>
                        <div class="tech-accuracy">精度 85.7%</div>
                        <div class="tech-description">
                            馬の成長曲線・調子の波を時系列で分析。調教・レース間隔・季節要因を総合判定。
                        </div>
                    </div>
                    <div class="tech-card">
                        <div class="tech-name">アンサンブル学習</div>
                        <div class="tech-accuracy">精度 88.4%</div>
                        <div class="tech-description">
                            複数モデルの予測を統合し、単体モデルの弱点を補完。安定した高精度予想を実現。
                        </div>
                    </div>
                    <div class="tech-card">
                        <div class="tech-name">深層ニューラルネット</div>
                        <div class="tech-accuracy">精度 89.1%</div>
                        <div class="tech-description">
                            非線形な複雑パターンを学習。人間では発見困難な隠れた相関関係を自動抽出。
                        </div>
                    </div>
                </div>
                
                <!-- AI技術詳細ボタン -->
                <div style="text-align: center; margin-top: 40px;">
                    <a href="/technology/" class="tech-detail-button" style="display: inline-block; background: linear-gradient(135deg, var(--accent-color) 0%, #8b5cf6 100%); color: white; padding: 15px 30px; border-radius: 8px; text-decoration: none; font-weight: 600; font-size: 1rem; transition: all 0.3s; box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);">
                        🔬 AI技術の詳細を見る
                    </a>
                </div>
            </section>

            <!-- Content Grid -->
            <div class="content-grid">
                <!-- Main Articles -->
                <section class="main-articles">
                    <article class="article-card">
                        <div class="article-content">
                            <span class="article-category">機械学習</span>
                            <h2 class="article-title">
                                <a href="/machine-learning/python-horse-racing-data-analysis/">Pythonで始める競馬データ分析 - pandas基礎からXGBoostまで</a>
                            </h2>
                            <p class="article-excerpt">
                                pandasとscikit-learnを使った競馬データ分析の基礎を解説。実際のコード例付きで初心者でも理解できます。
                            </p>
                            <div class="article-meta">
                                <span>📅 2025年01月15日</span>
                                <span>⏱️ 8分で読める</span>
                                <a href="/machine-learning/python-horse-racing-data-analysis/" class="read-more">続きを読む →</a>
                            </div>
                        </div>
                    </article>

                    <article class="article-card">
                        <div class="article-content">
                            <span class="article-category">深層学習</span>
                            <h2 class="article-title">
                                <a href="/deep-learning/lstm-time-series-tensorflow/">LSTMを使った時系列予想モデル - TensorFlow実装例</a>
                            </h2>
                            <p class="article-excerpt">
                                TensorFlowでLSTMネットワークを構築し、競馬の時系列データから予想モデルを作成する方法を詳解。
                            </p>
                            <div class="article-meta">
                                <span>📅 2025年01月10日</span>
                                <span>⏱️ 12分で読める</span>
                                <a href="/deep-learning/lstm-time-series-tensorflow/" class="read-more">続きを読む →</a>
                            </div>
                        </div>
                    </article>
                </section>

                <!-- Sidebar -->
                <aside class="sidebar">
                    <!-- Recent Results Summary -->
                    <div class="sidebar-card">
                        <h3 class="sidebar-title">📈 今週の累計実績</h3>
                        <div class="sidebar-item">
                            <div style="display: flex; justify-content: space-between;">
                                <span>総的中率</span>
                                <span style="color: var(--success-color); font-weight: 600;">75.0%</span>
                            </div>
                        </div>
                        <div class="sidebar-item">
                            <div style="display: flex; justify-content: space-between;">
                                <span>平均回収率</span>
                                <span style="color: var(--success-color); font-weight: 600;">148%</span>
                            </div>
                        </div>
                        <div class="sidebar-item">
                            <div style="display: flex; justify-content: space-between;">
                                <span>予想提供数</span>
                                <span style="color: var(--body-text-color); font-weight: 600;">60戦</span>
                            </div>
                        </div>
                        <div style="margin-top: 15px; text-align: center; font-size: 0.9rem;">
                            <div style="color: var(--success-color); font-weight: 600;">週間利益: +¥89,000</div>
                            <div style="color: var(--accent-text-color); margin-top: 3px;">(10万円投資時)</div>
                        </div>
                    </div>

                    <!-- Categories -->
                    <div class="sidebar-card">
                        <h3 class="sidebar-title">📚 コンテンツ</h3>
                        <div class="sidebar-item">
                            <a href="/machine-learning/" class="sidebar-link">機械学習</a>
                        </div>
                        <div class="sidebar-item">
                            <a href="/deep-learning/" class="sidebar-link">深層学習</a>
                        </div>
                        <div class="sidebar-item">
                            <a href="/data-science/" class="sidebar-link">データサイエンス</a>
                        </div>
                        <div class="sidebar-item">
                            <a href="/ai-predictions/" class="sidebar-link">AI予想</a>
                        </div>
                    </div>

                    <!-- Premium CTA -->
                    <div class="sidebar-card premium-cta">
                        <h3 class="sidebar-title">⭐ 有料AI予想サービス</h3>
                        <p style="color: var(--body-text-color); font-size: 0.95rem; margin-bottom: 15px; line-height: 1.5;">
                            <strong>詳細な買い目・投資金額・リスク分析</strong>を毎日配信。<br>
                            月間回収率150%超えの実績。
                        </p>
                        <div style="margin-bottom: 15px; font-size: 0.9rem; color: var(--accent-text-color);">
                            ✓ 毎日の詳細買い目配信<br>
                            ✓ リアルタイム相場分析<br>
                            ✓ 個別投資相談サポート
                        </div>
                        <a href="/premium" class="cta-button">
                            詳細を見る
                        </a>
                    </div>
                </aside>
            </div>
</BaseLayout>

<style>
        /* Hero Section - 投資訴求重視 */
        .hero-section {
            text-align: center;
            padding: 60px 40px;
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
            border-radius: var(--card-border-radius);
            margin-bottom: 40px;
            border: 1px solid var(--border-color);
        }
        
        .hero-stats-section {
            margin-top: 50px;
            padding-top: 30px;
            border-top: 1px solid rgba(148, 163, 184, 0.1);
        }

        .hero-title {
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-subtitle {
            font-size: 1.3rem;
            color: var(--body-text-color);
            margin-bottom: 30px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            font-weight: 500;
        }

        .hero-description {
            font-size: 1rem;
            color: var(--accent-text-color);
            margin-bottom: 40px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Live Prediction Section */
        .live-prediction-section {
            background: var(--card-background);
            padding: 40px;
            border-radius: var(--card-border-radius);
            margin: 40px 0;
            border: 1px solid var(--border-color);
        }

        .section-title {
            text-align: center;
            font-size: 1.8rem;
            color: var(--accent-color);
            margin-bottom: 15px;
            font-weight: 700;
        }

        .section-subtitle {
            text-align: center;
            color: var(--accent-text-color);
            margin-bottom: 30px;
        }

        .prediction-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .prediction-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 20px;
            border-radius: 8px;
            border: 1px solid var(--border-color);
            position: relative;
        }

        .race-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .race-title {
            font-weight: 700;
            color: var(--accent-color);
        }

        .confidence-score {
            background: var(--success-color);
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .prediction-summary {
            color: var(--body-text-color);
            margin-bottom: 15px;
            line-height: 1.5;
        }

        .blur-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 60%;
            background: linear-gradient(transparent 20%, rgba(15, 23, 42, 0.7) 40%, rgba(15, 23, 42, 0.95) 100%);
            border-radius: 0 0 8px 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            backdrop-filter: blur(2px);
        }

        .premium-badge {
            background: linear-gradient(135deg, var(--accent-color) 0%, #8b5cf6 100%);
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            font-size: 0.9rem;
            font-weight: 600;
        }

        /* AI Technology Section */
        .tech-section {
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
            padding: 40px;
            border-radius: var(--card-border-radius);
            margin: 40px 0;
            border: 1px solid var(--border-color);
        }

        .tech-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .tech-card {
            background: rgba(255, 255, 255, 0.05);
            padding: 25px;
            border-radius: 8px;
            border: 1px solid var(--border-color);
            transition: transform 0.3s;
        }

        .tech-card:hover {
            transform: translateY(-3px);
        }

        .tech-name {
            font-weight: 700;
            color: var(--accent-color);
            margin-bottom: 10px;
            font-size: 1.1rem;
        }

        .tech-accuracy {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--success-color);
            margin-bottom: 10px;
        }

        .tech-description {
            font-size: 0.9rem;
            color: var(--accent-text-color);
            line-height: 1.5;
        }

        /* Content Grid */
        .content-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 40px;
            margin-top: 40px;
        }

        .main-articles {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .article-card {
            background: var(--card-background);
            border-radius: var(--card-border-radius);
            border: 1px solid var(--border-color);
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .article-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(59, 130, 246, 0.2);
        }

        .article-content {
            padding: var(--card-padding);
        }

        .article-category {
            display: inline-block;
            background: var(--accent-color);
            color: white;
            padding: 4px 12px;
            border-radius: var(--tag-border-radius);
            font-size: 0.8rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .article-category.prediction {
            background: var(--success-color);
        }

        .article-category.tech {
            background: #8b5cf6;
        }

        .article-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 12px;
            color: var(--body-text-color);
        }

        .article-title a {
            color: inherit;
            text-decoration: none;
        }

        .article-title a:hover {
            color: var(--accent-color);
        }

        .article-excerpt {
            color: var(--accent-text-color);
            margin-bottom: 15px;
            line-height: 1.6;
        }

        .article-meta {
            display: flex;
            align-items: center;
            gap: 15px;
            font-size: 0.9rem;
            color: var(--accent-text-color);
        }

        .read-more {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: 600;
        }

        /* Sidebar */
        .sidebar {
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .sidebar-card {
            background: var(--card-background);
            padding: var(--card-padding);
            border-radius: var(--card-border-radius);
            border: 1px solid var(--border-color);
        }

        .sidebar-title {
            font-size: 1.2rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: var(--accent-color);
        }

        .sidebar-item {
            padding: 10px 0;
            border-bottom: 1px solid var(--border-color);
        }

        .sidebar-item:last-child {
            border-bottom: none;
        }

        .sidebar-link {
            color: var(--body-text-color);
            text-decoration: none;
            font-weight: 500;
        }

        .sidebar-link:hover {
            color: var(--accent-color);
        }

        /* Premium CTA */
        .premium-cta {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.2) 0%, rgba(139, 92, 246, 0.2) 100%);
            border: 1px solid rgba(59, 130, 246, 0.4);
        }

        .cta-button {
            display: inline-block;
            background: var(--accent-color);
            color: white;
            padding: 12px 20px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: transform 0.3s;
        }

        .cta-button:hover {
            transform: translateY(-2px);
        }

        /* Tech Detail Button */
        .tech-detail-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4) !important;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.2rem;
            }

            .content-grid {
                grid-template-columns: 1fr;
                gap: 30px;
            }

            .prediction-cards, .tech-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-grid {
                grid-template-columns: 1fr !important;
                gap: 20px !important;
                padding: 0 !important;
            }
            
            
            
            .hero-section {
                padding: 40px 20px !important;
            }
            
            .hero-stats-section {
                margin-top: 30px !important;
            }
            
            .stats-card {
                padding: 18px 12px !important;
            }
            
            .stats-number {
                font-size: 2rem !important;
            }
        }
        

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
        
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }

        /* Footer Styles */
        .footer {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            border-top: 1px solid rgba(59, 130, 246, 0.2);
            padding: 40px 0 20px;
            margin-top: 80px;
            color: #cbd5e1;
        }

        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-section h3 {
            color: #3b82f6;
            font-size: 1.2rem;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .footer-section ul {
            list-style: none;
            padding: 0;
        }

        .footer-section li {
            margin-bottom: 12px;
        }

        .footer-section a {
            color: #94a3b8;
            text-decoration: none;
            transition: color 0.3s ease;
            font-size: 0.95rem;
        }

        .footer-section a:hover {
            color: #3b82f6;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(59, 130, 246, 0.1);
            color: #6b7280;
            font-size: 0.9rem;
        }

        .company-info {
            color: #94a3b8;
            line-height: 1.6;
        }

        @media (max-width: 768px) {
            .footer-content {
                grid-template-columns: 1fr;
                gap: 30px;
            }
        }
</style>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>サービス</h3>
                    <ul>
                        <li><a href="/free-prediction">無料予想</a></li>
                        <li><a href="/pricing">料金プラン</a></li>
                        <li><a href="/technology">AI技術</a></li>
                        <li><a href="/beginner">初心者ガイド</a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h3>サポート</h3>
                    <ul>
                        <li><a href="/dashboard">ダッシュボード</a></li>
                        <li><a href="/account">アカウント設定</a></li>
                        <li><a href="/blog">ブログ</a></li>
                        <li><a href="mailto:support@keiba.link">お問い合わせ</a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h3>法的情報</h3>
                    <ul>
                        <li><a href="/terms">利用規約</a></li>
                        <li><a href="/privacy">プライバシーポリシー</a></li>
                        <li><a href="/tokutei">特定商取引法に基づく表記</a></li>
                    </ul>
                    <div class="company-info" style="margin-top: 25px;">
                        <strong>株式会社リリーフ</strong><br>
                        NANKANアナリティクス運営事務局<br>
                        〒771-0130<br>
                        徳島県徳島市川内町加賀須野550番地2
                    </div>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>© 2025 株式会社リリーフ NANKANアナリティクス. All rights reserved.</p>
                <p style="margin-top: 10px; font-size: 0.8rem; opacity: 0.8;">
                    当サービスはデータ分析学習を目的とした教育コンテンツです。競馬は自己責任でお楽しみください。
                </p>
            </div>
        </div>
    </footer>

<script>
document.addEventListener("DOMContentLoaded",function(){
    function s(t,e,r,i,a=""){
        if(!t||!t.getAttribute)return;
        t.innerHTML="0"+a;
        let l=null;
        const u=g=>{
            l||(l=g);
            const d=Math.min((g-l)/i,1);
            let c;
            try{
                r>=1e3?(c=Math.floor(d*(r-e)+e),t.innerHTML=c.toLocaleString()+a):(c=d*(r-e)+e,t.innerHTML=c.toFixed(1)+a),d<1&&window.requestAnimationFrame(u)
            }catch(f){
                console.error("カウントアップエラー:",f),t.innerHTML=r+a
            }
        };
        window.requestAnimationFrame(u)
    }

    const o=new IntersectionObserver(t=>{
        t.forEach(e=>{
            if(e.isIntersecting&&!e.target.hasAttribute("data-animated")){
                const r=e.target.getAttribute("data-target"),
                      i=e.target.getAttribute("data-suffix")||"";
                if(!r){
                    console.warn("data-target属性が見つかりません:",e.target);
                    return
                }
                const a=parseFloat(r);
                if(isNaN(a)){
                    console.warn("無効なdata-target値:",r);
                    return
                }
                console.log("カウントアップ開始:",a,i),
                s(e.target,0,a,2e3,i),
                e.target.setAttribute("data-animated","true")
            }
        })
    },{threshold:.3});

    const n=document.querySelectorAll(".stats-number");
    console.log("stats-number要素を発見:",n.length);
    n.forEach(t=>{
        const e=t.getAttribute("data-target");
        console.log("要素:",t,"data-target:",e),
        o.observe(t)
    })
});
</script>
</body>
</html>
