---
export const prerender = true;
import BaseLayout from '../layouts/BaseLayout.astro';

const title = 'AI自動絞り込み機能 実体験 | NANKANアナリティクス';
const description = '購入前に実際の的中レースで体験！AI自動絞り込み機能の実体験。累積スコアベースの自動戦略選択で、わずか9点の少点数買い目を実現。';

// 固定デモデータ（2025年9月3日 大井競馬11R アフター５スター賞）
const demoData = {
    raceDate: '2025年9月3日',
    track: '大井競馬',
    raceNumber: '11R',
    raceName: 'アフター５スター賞（SIII）',
    distance: 'ダ1,200m',
    horseCount: 16,
    startTime: '20:10',

    // 馬データ
    main: {
        number: 11,
        name: 'ファーンヒル',
        role: '本命',
        confidence: 88,
        rating: '★★★',
        features: {
            stability: 85,
            ability: 85,
            pace: 94
        }
    },
    sub: {
        number: 1,
        name: 'マックス',
        role: '対抗',
        confidence: 85,
        rating: '★★★',
        features: {
            stability: 84,
            ability: 85,
            pace: 86
        }
    },
    minor: [
        { number: 2, name: 'トーセンサンダー', role: '単穴', confidence: 82, rating: '★★★' },
        { number: 12, name: 'ドリームビリーバー', role: '単穴', confidence: 75, rating: '★★★' }
    ],
    low: [
        { number: 15, name: 'オメガレインボー', confidence: 74 },
        { number: 4, name: 'ジゼル', confidence: 73 },
        { number: 3, name: 'リオンダリーナ', confidence: 71 },
        { number: 9, name: 'シーサーペント', confidence: 71 }
    ],
    other: [
        { number: 7, name: 'ポリゴンウェイヴ', confidence: 70 },
        { number: 10, name: 'ボヌールバローズ', confidence: 67 },
        { number: 14, name: 'スワーヴシャルル', confidence: 66 },
        { number: 16, name: 'ワラ', confidence: 66 }
    ],

    // AI三連複買い目
    sanrenpukuBets: [
        '11-1-2.3.4.12.15',
        '11-2-3.4.12.15'
    ],
    betCount: 9,

    // 的中結果
    result: {
        order: '2-11-12',
        payout: 36900,
        isHit: true
    }
};

// 累積スコア基準の戦略選択ロジック
const mainScore = demoData.main.confidence;
let selectedStrategy = '';
let strategyDescription = '';

if (mainScore >= 89) {
    selectedStrategy = '少点数型+バランス型ミックス';
    strategyDescription = '累積スコア89pt以上。本命の信頼度が非常に高いため、少点数型とバランス型の上位5点のみに絞り込みます。';
} else if (mainScore === 88) {
    selectedStrategy = '少点数型寄り';
    strategyDescription = '累積スコア88pt。本命の信頼度が高いため、少点数型を中心に7点に絞り込みます。';
} else if (mainScore === 87) {
    selectedStrategy = 'バランス型';
    strategyDescription = '累積スコア87pt。バランスの取れた配置のため、バランス型10点を選択します。';
} else if (mainScore === 86) {
    selectedStrategy = 'バランス型+高配当型ミックス';
    strategyDescription = '累積スコア86pt。若干波乱含みのため、バランス型と高配当型から10点選択します。';
} else {
    selectedStrategy = '高配当型';
    strategyDescription = '累積スコア85pt以下。穴狙いの配置のため、高配当型10点を選択します。';
}

// 今回のデモケースは88ptなので「少点数型寄り 9点」
const actualStrategy = '少点数型寄り';
const actualDescription = '本命ファーンヒル（88pt）の信頼度が高いため、少点数型を中心に9点に絞り込みました。';
---

<BaseLayout title={title} description={description}>
    <div class="sample-page-container">
        <!-- ヘッダー -->
        <div class="sample-header">
            <div class="badge-new">🎯 無料実体験</div>
            <h1>AI自動絞り込み機能 実体験</h1>
            <p class="header-description">
                購入前に、実際のレースでPremium Sanrenpukuの絞り込み機能を実体験できます。
            </p>
        </div>

        <!-- レース情報 -->
        <div class="sample-race-info">
            <h2>📅 2025年9月3日　アフター５スター賞（SIII）</h2>
            <div class="race-meta">
                <span class="meta-item">🏇 {demoData.track} {demoData.raceNumber}</span>
                <span class="meta-item">📏 {demoData.distance}</span>
                <span class="meta-item">🐎 {demoData.horseCount}頭立て</span>
                <span class="meta-item">🕐 発走時刻 {demoData.startTime}</span>
            </div>
        </div>

        <!-- AI自動判定の説明 -->
        <div class="auto-selection-explanation">
            <h2>🤖 AI自動判定の仕組み</h2>
            <div class="explanation-content">
                <div class="explanation-step">
                    <span class="step-number">1</span>
                    <div class="step-content">
                        <h3>累積スコアを計算</h3>
                        <p>本命馬の累積スコア（信頼度）を基準に、レース全体の難易度を判定します。</p>
                        <div class="score-example">
                            <span class="score-label">本命 {demoData.main.name}</span>
                            <span class="score-value">{demoData.main.confidence}pt</span>
                        </div>
                    </div>
                </div>

                <div class="explanation-step">
                    <span class="step-number">2</span>
                    <div class="step-content">
                        <h3>最適戦略を自動選択</h3>
                        <p>スコアに応じて、少点数型・バランス型・高配当型から最適な戦略を自動判定します。</p>
                        <div class="strategy-selected">
                            <span class="strategy-icon">✅</span>
                            <span class="strategy-name">{actualStrategy}</span>
                        </div>
                        <p class="strategy-reason">{actualDescription}</p>
                    </div>
                </div>

                <div class="explanation-step">
                    <span class="step-number">3</span>
                    <div class="step-content">
                        <h3>買い目を自動絞り込み</h3>
                        <p>選択された戦略の買い目を、累積スコア順にソートして上位のみを抽出します。</p>
                        <div class="bet-count">
                            <span class="count-icon">🎯</span>
                            <span class="count-value">{demoData.betCount}点</span>
                            <span class="count-label">に絞り込み</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 出走馬分析 -->
        <div class="horses-analysis">
            <h2>🐎 出走馬分析</h2>

            <!-- 本命 -->
            <div class="horse-card main">
                <div class="horse-header">
                    <span class="horse-mark">◎</span>
                    <span class="horse-number">{demoData.main.number}</span>
                    <h3 class="horse-name">{demoData.main.name}</h3>
                    <span class="horse-role">{demoData.main.role}</span>
                </div>
                <div class="horse-details">
                    <div class="horse-rating">
                        <span class="rating-label">総合評価:</span>
                        <span class="rating-stars">{demoData.main.rating}</span>
                    </div>
                    <div class="horse-score">
                        <span class="score-label">累積スコア:</span>
                        <span class="score-value">{demoData.main.confidence}pt</span>
                    </div>
                    <div class="feature-importance">
                        <h4>特徴量重要度</h4>
                        <div class="feature-bar">
                            <span class="feature-label">安定性</span>
                            <div class="feature-progress">
                                <div class="feature-fill" style={`width: ${demoData.main.features.stability}%`}></div>
                            </div>
                            <span class="feature-value">{demoData.main.features.stability}%</span>
                        </div>
                        <div class="feature-bar">
                            <span class="feature-label">能力上位性</span>
                            <div class="feature-progress">
                                <div class="feature-fill" style={`width: ${demoData.main.features.ability}%`}></div>
                            </div>
                            <span class="feature-value">{demoData.main.features.ability}%</span>
                        </div>
                        <div class="feature-bar">
                            <span class="feature-label">展開利</span>
                            <div class="feature-progress">
                                <div class="feature-fill" style={`width: ${demoData.main.features.pace}%`}></div>
                            </div>
                            <span class="feature-value">{demoData.main.features.pace}%</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 対抗 -->
            <div class="horse-card sub">
                <div class="horse-header">
                    <span class="horse-mark">○</span>
                    <span class="horse-number">{demoData.sub.number}</span>
                    <h3 class="horse-name">{demoData.sub.name}</h3>
                    <span class="horse-role">{demoData.sub.role}</span>
                </div>
                <div class="horse-details">
                    <div class="horse-rating">
                        <span class="rating-label">総合評価:</span>
                        <span class="rating-stars">{demoData.sub.rating}</span>
                    </div>
                    <div class="horse-score">
                        <span class="score-label">累積スコア:</span>
                        <span class="score-value">{demoData.sub.confidence}pt</span>
                    </div>
                    <div class="feature-importance">
                        <h4>特徴量重要度</h4>
                        <div class="feature-bar">
                            <span class="feature-label">安定性</span>
                            <div class="feature-progress">
                                <div class="feature-fill" style={`width: ${demoData.sub.features.stability}%`}></div>
                            </div>
                            <span class="feature-value">{demoData.sub.features.stability}%</span>
                        </div>
                        <div class="feature-bar">
                            <span class="feature-label">能力上位性</span>
                            <div class="feature-progress">
                                <div class="feature-fill" style={`width: ${demoData.sub.features.ability}%`}></div>
                            </div>
                            <span class="feature-value">{demoData.sub.features.ability}%</span>
                        </div>
                        <div class="feature-bar">
                            <span class="feature-label">展開利</span>
                            <div class="feature-progress">
                                <div class="feature-fill" style={`width: ${demoData.sub.features.pace}%`}></div>
                            </div>
                            <span class="feature-value">{demoData.sub.features.pace}%</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- 単穴 -->
            {demoData.minor.map(horse => (
                <div class="horse-card minor">
                    <div class="horse-header">
                        <span class="horse-mark">▲</span>
                        <span class="horse-number">{horse.number}</span>
                        <h3 class="horse-name">{horse.name}</h3>
                        <span class="horse-role">{horse.role}</span>
                    </div>
                    <div class="horse-details">
                        <div class="horse-rating">
                            <span class="rating-label">総合評価:</span>
                            <span class="rating-stars">{horse.rating}</span>
                        </div>
                        <div class="horse-score">
                            <span class="score-label">累積スコア:</span>
                            <span class="score-value">{horse.confidence}pt</span>
                        </div>
                    </div>
                </div>
            ))}

            <!-- 連下候補馬 -->
            <div class="horse-group">
                <h3 class="group-title">△ 連下候補馬</h3>
                <div class="horse-list">
                    {demoData.low.map(horse => (
                        <div class="horse-item">
                            <span class="horse-number">{horse.number}</span>
                            <span class="horse-name">{horse.name}</span>
                            <span class="horse-score">({horse.confidence}pt)</span>
                        </div>
                    ))}
                </div>
            </div>

            <!-- 抑え候補馬 -->
            <div class="horse-group">
                <h3 class="group-title">× 抑え候補馬</h3>
                <div class="horse-list">
                    {demoData.other.map(horse => (
                        <div class="horse-item">
                            <span class="horse-number">{horse.number}</span>
                            <span class="horse-name">{horse.name}</span>
                            <span class="horse-score">({horse.confidence}pt)</span>
                        </div>
                    ))}
                </div>
            </div>
        </div>

        <!-- AI絞り込みボタン -->
        <button class="demo-filter-btn" id="demoFilterBtn">
            <span class="filter-icon">🎯</span>
            <span class="filter-text">AI三連複 絞り込む</span>
            <span class="filter-arrow">▼</span>
        </button>

        <!-- AI絞り込み買い目（初期非表示） -->
        <div class="filtered-bets" id="demoContent" style="display: none;">
            <h2>🎯 AI自動絞り込み買い目</h2>
            <div class="bet-strategy-info">
                <span class="strategy-badge">{actualStrategy}</span>
                <span class="bet-count-badge">{demoData.betCount}点</span>
            </div>

            <div class="sanrenpuku-bets">
                <h3>三連複買い目</h3>
                <div class="bet-list">
                    {demoData.sanrenpukuBets.map(bet => (
                        <div class="bet-item">
                            <span class="bet-formula">{bet}</span>
                        </div>
                    ))}
                </div>
                <div class="bet-total">
                    <span class="total-label">計</span>
                    <span class="total-count">{demoData.betCount}点</span>
                </div>
            </div>

            <!-- 的中結果 -->
            {demoData.result.isHit && (
            <div class="result-section hit">
                <div class="result-header">
                    <h2>🎊 的中結果</h2>
                    <span class="hit-badge">的中！</span>
                </div>
                <div class="result-details">
                    <div class="result-order">
                        <span class="order-label">着順</span>
                        <span class="order-value">{demoData.result.order}</span>
                    </div>
                    <div class="result-payout">
                        <span class="payout-label">払い戻し</span>
                        <span class="payout-value">¥{demoData.result.payout.toLocaleString()}</span>
                    </div>
                </div>
                <p class="result-note">
                    💰 この買い目で実際に{demoData.betCount}点購入した場合、<br>
                    投資額 ¥{(demoData.betCount * 100).toLocaleString()} に対して<br>
                    払い戻し ¥{demoData.result.payout.toLocaleString()} = <strong>回収率{Math.round(demoData.result.payout / (demoData.betCount * 100) * 100)}%</strong>
                </p>
            </div>
        )}

        <!-- CTA -->
        <div class="cta-section">
            <h2>💎 Premium Sanrenpukuで毎日この精度の予想を</h2>
            <p class="cta-description">
                今回体験いただいたAI三連複絞り込み機能を、<br>
                全レース・毎日の開催でご利用いただけます。
            </p>
            <div class="cta-features">
                <div class="cta-feature">
                    <span class="feature-icon">🎯</span>
                    <h3>AI自動絞り込み</h3>
                    <p>累積スコアベースの自動戦略選択</p>
                </div>
                <div class="cta-feature">
                    <span class="feature-icon">📊</span>
                    <h3>的中率78%</h3>
                    <p>過去1年間の実績データ</p>
                </div>
                <div class="cta-feature">
                    <span class="feature-icon">💰</span>
                    <h3>回収率178%</h3>
                    <p>投資額の約1.8倍回収</p>
                </div>
            </div>
            <div class="cta-plan-selection">
                <h3 class="plan-selection-title">🔄 プラン変更をお選びください</h3>
                <p class="plan-selection-note">
                    ※ AI自動絞り込み機能は「三連複」のみです。馬単は従来通り3戦略の買い目提供となります。
                </p>
                <div class="plan-buttons">
                    <a href="https://buy.stripe.com/6oU28r90R1W8c0jbB1dby0Q" class="plan-btn combo-plan">
                        <div class="plan-badge">おすすめ</div>
                        <div class="plan-name">馬単+三連複 併用プランに変更</div>
                        <div class="plan-subtitle">Premium Combo</div>
                        <div class="plan-price">¥24,800<span class="plan-period">/月</span></div>
                        <div class="plan-note">通常¥29,800 → ¥5,000 OFF</div>
                        <div class="plan-features">
                            <div class="feature-item">
                                <strong>馬単（3戦略）</strong><br>
                                <span class="feature-detail">引き続きご利用いただけます</span>
                            </div>
                            <div class="feature-divider">+</div>
                            <div class="feature-item">
                                <strong>三連複（AI絞り込み）</strong><br>
                                <span class="feature-detail">新たに追加されます</span>
                            </div>
                        </div>
                    </a>
                    <a href="https://buy.stripe.com/28EcN5fpf58k8O734vdby0P" class="plan-btn single-plan">
                        <div class="plan-name">三連複のみプランに変更</div>
                        <div class="plan-subtitle">Premium Sanrenpuku</div>
                        <div class="plan-price">¥19,820<span class="plan-period">/月</span></div>
                        <div class="plan-note">馬単から三連複に完全切り替え</div>
                        <div class="plan-features">
                            <div class="feature-item">
                                <strong>三連複（AI絞り込み）</strong><br>
                                <span class="feature-detail">本日体験した機能のみ</span>
                            </div>
                            <div class="feature-note">
                                ※ 馬単の3戦略買い目は含まれません
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
</BaseLayout>

<style>
    .sample-page-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    /* ヘッダー */
    .sample-header {
        text-align: center;
        margin-bottom: 60px;
    }

    .badge-new {
        display: inline-block;
        padding: 8px 20px;
        background: linear-gradient(135deg, #f59e0b, #ea580c);
        color: white;
        border-radius: 20px;
        font-weight: 600;
        margin-bottom: 20px;
        font-size: 0.9rem;
    }

    .sample-header h1 {
        font-size: 2.5rem;
        margin-bottom: 20px;
        background: linear-gradient(135deg, #3b82f6, #8b5cf6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .header-description {
        font-size: 1.1rem;
        color: #94a3b8;
        line-height: 1.8;
    }

    /* レース情報 */
    .sample-race-info {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
        border: 1px solid rgba(59, 130, 246, 0.2);
        border-radius: 16px;
        padding: 30px;
        margin-bottom: 40px;
    }

    .sample-race-info h2 {
        font-size: 1.5rem;
        margin-bottom: 20px;
        color: #60a5fa;
    }

    .race-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 15px;
        margin-bottom: 15px;
    }

    .meta-item {
        background: rgba(255, 255, 255, 0.05);
        padding: 8px 16px;
        border-radius: 8px;
        font-size: 0.9rem;
        color: #cbd5e1;
    }

    .race-name {
        font-size: 1.1rem;
        color: #e2e8f0;
        font-weight: 600;
    }

    /* AI自動判定説明 */
    .auto-selection-explanation {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.1), rgba(6, 182, 212, 0.1));
        border: 1px solid rgba(16, 185, 129, 0.2);
        border-radius: 16px;
        padding: 30px;
        margin-bottom: 40px;
    }

    .auto-selection-explanation h2 {
        font-size: 1.5rem;
        margin-bottom: 30px;
        color: #10b981;
    }

    .explanation-content {
        display: grid;
        gap: 30px;
    }

    .explanation-step {
        display: flex;
        gap: 20px;
        align-items: flex-start;
    }

    .step-number {
        flex-shrink: 0;
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, #10b981, #06b6d4);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 700;
        font-size: 1.2rem;
    }

    .step-content h3 {
        font-size: 1.2rem;
        margin-bottom: 10px;
        color: #e2e8f0;
    }

    .step-content p {
        color: #94a3b8;
        line-height: 1.6;
        margin-bottom: 15px;
    }

    .score-example,
    .strategy-selected,
    .bet-count {
        background: rgba(255, 255, 255, 0.05);
        padding: 12px 20px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        gap: 10px;
        margin-top: 10px;
    }

    .score-label,
    .strategy-name,
    .count-label {
        color: #cbd5e1;
    }

    .score-value,
    .count-value {
        color: #10b981;
        font-weight: 700;
        font-size: 1.2rem;
    }

    .strategy-icon,
    .count-icon {
        font-size: 1.5rem;
    }

    .strategy-reason {
        margin-top: 10px;
        font-size: 0.9rem;
        color: #94a3b8;
        font-style: italic;
    }

    /* 出走馬分析 */
    .horses-analysis {
        margin-bottom: 40px;
    }

    .horses-analysis > h2 {
        font-size: 1.8rem;
        margin-bottom: 30px;
        color: #e2e8f0;
    }

    .horse-card {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.05));
        border: 1px solid rgba(59, 130, 246, 0.2);
        border-radius: 12px;
        padding: 24px;
        margin-bottom: 20px;
    }

    .horse-card.main {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.15), rgba(16, 185, 129, 0.05));
        border-color: rgba(16, 185, 129, 0.3);
    }

    .horse-card.sub {
        background: linear-gradient(135deg, rgba(236, 72, 153, 0.15), rgba(236, 72, 153, 0.05));
        border-color: rgba(236, 72, 153, 0.3);
    }

    .horse-card.minor {
        background: linear-gradient(135deg, rgba(251, 191, 36, 0.15), rgba(251, 191, 36, 0.05));
        border-color: rgba(251, 191, 36, 0.3);
    }

    .horse-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 20px;
    }

    .horse-mark {
        font-size: 1.5rem;
    }

    .horse-number {
        background: rgba(59, 130, 246, 0.2);
        color: #60a5fa;
        padding: 4px 12px;
        border-radius: 6px;
        font-weight: 700;
    }

    .horse-name {
        font-size: 1.3rem;
        color: #e2e8f0;
        margin: 0;
    }

    .horse-role {
        background: rgba(139, 92, 246, 0.2);
        color: #a78bfa;
        padding: 4px 12px;
        border-radius: 6px;
        font-size: 0.85rem;
    }

    .horse-details {
        display: grid;
        gap: 15px;
    }

    .horse-rating,
    .horse-score {
        display: flex;
        gap: 10px;
        align-items: center;
    }

    .rating-label,
    .score-label {
        color: #94a3b8;
        font-size: 0.9rem;
    }

    .rating-stars {
        color: #fbbf24;
        font-size: 1.1rem;
    }

    .score-value {
        color: #10b981;
        font-weight: 700;
        font-size: 1.1rem;
    }

    .feature-importance h4 {
        font-size: 0.95rem;
        color: #cbd5e1;
        margin-bottom: 12px;
    }

    .feature-bar {
        display: grid;
        grid-template-columns: 80px 1fr 50px;
        gap: 10px;
        align-items: center;
        margin-bottom: 8px;
    }

    .feature-label {
        font-size: 0.85rem;
        color: #94a3b8;
    }

    .feature-progress {
        background: rgba(255, 255, 255, 0.05);
        height: 8px;
        border-radius: 4px;
        overflow: hidden;
    }

    .feature-fill {
        height: 100%;
        background: linear-gradient(90deg, #10b981, #06b6d4);
        border-radius: 4px;
        transition: width 0.3s ease;
    }

    .feature-value {
        font-size: 0.85rem;
        color: #10b981;
        font-weight: 600;
        text-align: right;
    }

    /* 連下・抑え候補馬 */
    .horse-group {
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 20px;
    }

    .group-title {
        font-size: 1.1rem;
        color: #cbd5e1;
        margin-bottom: 15px;
    }

    .horse-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 10px;
    }

    .horse-item {
        display: flex;
        gap: 8px;
        align-items: center;
        padding: 8px 12px;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 6px;
    }

    .horse-item .horse-number {
        background: rgba(139, 92, 246, 0.2);
        color: #a78bfa;
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 0.85rem;
        font-weight: 600;
    }

    .horse-item .horse-name {
        color: #e2e8f0;
        font-size: 0.9rem;
    }

    .horse-item .horse-score {
        color: #94a3b8;
        font-size: 0.85rem;
        margin-left: auto;
    }

    /* AI絞り込み買い目 */
    .filtered-bets {
        background: linear-gradient(135deg, rgba(245, 158, 11, 0.15), rgba(234, 88, 12, 0.1));
        border: 1px solid rgba(245, 158, 11, 0.3);
        border-radius: 16px;
        padding: 30px;
        margin-bottom: 40px;
    }

    .filtered-bets h2 {
        font-size: 1.8rem;
        margin-bottom: 20px;
        color: #fbbf24;
    }

    .bet-strategy-info {
        display: flex;
        gap: 12px;
        margin-bottom: 25px;
    }

    .strategy-badge,
    .bet-count-badge {
        padding: 8px 16px;
        border-radius: 8px;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .strategy-badge {
        background: rgba(245, 158, 11, 0.2);
        color: #fbbf24;
    }

    .bet-count-badge {
        background: rgba(16, 185, 129, 0.2);
        color: #10b981;
    }

    .sanrenpuku-bets h3 {
        font-size: 1.2rem;
        color: #e2e8f0;
        margin-bottom: 15px;
    }

    .bet-list {
        background: rgba(0, 0, 0, 0.2);
        border-radius: 8px;
        padding: 20px;
        margin-bottom: 15px;
    }

    .bet-item {
        padding: 12px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .bet-item:last-child {
        border-bottom: none;
    }

    .bet-formula {
        color: #fbbf24;
        font-size: 1.1rem;
        font-weight: 600;
        font-family: 'Courier New', monospace;
    }

    .bet-total {
        display: flex;
        gap: 10px;
        justify-content: flex-end;
        padding-top: 10px;
        border-top: 2px solid rgba(245, 158, 11, 0.3);
    }

    .total-label {
        color: #94a3b8;
        font-size: 1.1rem;
    }

    .total-count {
        color: #fbbf24;
        font-size: 1.1rem;
        font-weight: 700;
    }

    /* 的中結果 */
    .result-section {
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.2), rgba(6, 182, 212, 0.1));
        border: 2px solid rgba(16, 185, 129, 0.4);
        border-radius: 16px;
        padding: 30px;
        margin-bottom: 40px;
    }

    .result-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 25px;
    }

    .result-header h2 {
        font-size: 1.8rem;
        color: #10b981;
        margin: 0;
    }

    .hit-badge {
        background: linear-gradient(135deg, #10b981, #06b6d4);
        color: white;
        padding: 10px 24px;
        border-radius: 20px;
        font-weight: 700;
        font-size: 1.1rem;
        animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }

    .result-details {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 20px;
    }

    .result-order,
    .result-payout {
        background: rgba(0, 0, 0, 0.2);
        border-radius: 12px;
        padding: 20px;
        text-align: center;
    }

    .order-label,
    .payout-label {
        display: block;
        color: #94a3b8;
        font-size: 0.9rem;
        margin-bottom: 10px;
    }

    .order-value {
        display: block;
        color: #10b981;
        font-size: 2rem;
        font-weight: 700;
        font-family: 'Courier New', monospace;
    }

    .payout-value {
        display: block;
        color: #fbbf24;
        font-size: 2.2rem;
        font-weight: 700;
    }

    .result-note {
        background: rgba(255, 255, 255, 0.05);
        border-left: 4px solid #10b981;
        padding: 15px 20px;
        border-radius: 8px;
        color: #cbd5e1;
        line-height: 1.8;
    }

    .result-note strong {
        color: #10b981;
        font-size: 1.2rem;
    }

    /* CTA */
    .cta-section {
        text-align: center;
        padding: 50px 30px;
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
        border: 1px solid rgba(59, 130, 246, 0.2);
        border-radius: 16px;
    }

    .cta-section h2 {
        font-size: 1.8rem;
        margin-bottom: 15px;
        color: #60a5fa;
    }

    .cta-description {
        font-size: 1.1rem;
        color: #94a3b8;
        margin-bottom: 40px;
        line-height: 1.8;
    }

    .cta-features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 30px;
        margin-bottom: 40px;
    }

    .cta-feature {
        padding: 20px;
    }

    .feature-icon {
        font-size: 3rem;
        display: block;
        margin-bottom: 15px;
    }

    .cta-feature h3 {
        font-size: 1.2rem;
        color: #e2e8f0;
        margin-bottom: 10px;
    }

    .cta-feature p {
        color: #94a3b8;
        font-size: 0.95rem;
    }

    .cta-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn-primary,
    .btn-secondary {
        padding: 16px 40px;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1.1rem;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-block;
    }

    .btn-primary {
        background: linear-gradient(135deg, #3b82f6, #8b5cf6);
        color: white;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(59, 130, 246, 0.4);
    }

    .btn-secondary {
        background: rgba(255, 255, 255, 0.05);
        border: 2px solid rgba(59, 130, 246, 0.3);
        color: #60a5fa;
    }

    .btn-secondary:hover {
        background: rgba(59, 130, 246, 0.1);
        border-color: rgba(59, 130, 246, 0.5);
    }

    /* プラン選択セクション */
    .cta-plan-selection {
        margin-top: 40px;
    }

    .plan-selection-title {
        font-size: 1.5rem;
        color: #e2e8f0;
        text-align: center;
        margin-bottom: 15px;
    }

    .plan-selection-note {
        text-align: center;
        color: #fbbf24;
        font-size: 0.95rem;
        margin-bottom: 30px;
        padding: 12px 20px;
        background: rgba(251, 191, 36, 0.1);
        border-left: 4px solid #fbbf24;
        border-radius: 8px;
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
    }

    .plan-buttons {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 30px;
        max-width: 900px;
        margin: 0 auto;
    }

    .plan-btn {
        position: relative;
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(139, 92, 246, 0.1));
        border: 2px solid rgba(59, 130, 246, 0.3);
        border-radius: 16px;
        padding: 30px 25px;
        text-decoration: none;
        transition: all 0.3s ease;
        display: block;
        text-align: center;
    }

    .plan-btn:hover {
        transform: translateY(-4px);
        box-shadow: 0 12px 30px rgba(59, 130, 246, 0.3);
        border-color: rgba(59, 130, 246, 0.5);
    }

    .combo-plan {
        background: linear-gradient(135deg, rgba(251, 191, 36, 0.15), rgba(245, 158, 11, 0.1));
        border-color: rgba(251, 191, 36, 0.4);
    }

    .combo-plan:hover {
        box-shadow: 0 12px 30px rgba(251, 191, 36, 0.4);
        border-color: rgba(251, 191, 36, 0.6);
    }

    .plan-badge {
        position: absolute;
        top: -12px;
        right: 20px;
        background: linear-gradient(135deg, #fbbf24, #f59e0b);
        color: #000;
        padding: 6px 16px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 700;
    }

    .plan-name {
        font-size: 1.3rem;
        font-weight: 700;
        color: #e2e8f0;
        margin-bottom: 8px;
    }

    .plan-subtitle {
        font-size: 0.9rem;
        color: #94a3b8;
        margin-bottom: 15px;
    }

    .plan-price {
        font-size: 2.5rem;
        font-weight: 700;
        color: #fbbf24;
        margin-bottom: 10px;
    }

    .combo-plan .plan-price {
        color: #fbbf24;
    }

    .single-plan .plan-price {
        color: #60a5fa;
    }

    .plan-period {
        font-size: 1rem;
        color: #94a3b8;
        font-weight: 400;
    }

    .plan-note {
        font-size: 0.9rem;
        color: #94a3b8;
        margin-bottom: 20px;
    }

    .plan-features {
        background: rgba(0, 0, 0, 0.2);
        border-radius: 8px;
        padding: 20px;
        font-size: 0.95rem;
        color: #cbd5e1;
        line-height: 1.8;
        text-align: center;
    }

    .feature-item {
        margin-bottom: 10px;
    }

    .feature-item strong {
        color: #e2e8f0;
        font-size: 1.05rem;
    }

    .feature-detail {
        color: #94a3b8;
        font-size: 0.85rem;
    }

    .feature-divider {
        font-size: 1.5rem;
        color: #fbbf24;
        margin: 15px 0;
        font-weight: 700;
    }

    .feature-note {
        margin-top: 15px;
        padding-top: 15px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        color: #f59e0b;
        font-size: 0.9rem;
    }

    /* AI絞り込みボタン */
    .demo-filter-btn {
        width: 100%;
        background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
        border: none;
        border-radius: 12px;
        padding: 18px 25px;
        color: #000;
        font-size: 1.1rem;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        box-shadow: 0 4px 15px rgba(251, 191, 36, 0.3);
        margin-bottom: 40px;
    }

    .demo-filter-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(251, 191, 36, 0.4);
    }

    .demo-filter-btn.filtering {
        background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
        animation: pulse 1s ease-in-out infinite;
    }

    .demo-filter-btn.filtered {
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    .filter-icon {
        font-size: 1.5rem;
    }

    .filter-arrow {
        transition: transform 0.3s ease;
    }

    .demo-filter-btn.filtered .filter-arrow {
        transform: rotate(180deg);
    }

    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.7; }
    }

    /* レスポンシブ */
    @media (max-width: 768px) {
        .sample-header h1 {
            font-size: 1.8rem;
        }

        .race-meta {
            flex-direction: column;
        }

        .explanation-step {
            flex-direction: column;
        }

        .horse-list {
            grid-template-columns: 1fr;
        }

        .plan-buttons {
            grid-template-columns: 1fr;
            gap: 20px;
        }

        .plan-selection-title {
            font-size: 1.3rem;
        }

        .plan-price {
            font-size: 2rem;
        }

        .demo-filter-btn {
            padding: 16px 20px;
            font-size: 1rem;
        }
    }
</style>

<script is:inline>
document.addEventListener('DOMContentLoaded', function() {
    const filterBtn = document.getElementById('demoFilterBtn');
    const demoContent = document.getElementById('demoContent');

    if (filterBtn && demoContent) {
        filterBtn.addEventListener('click', function() {
            const filterText = this.querySelector('.filter-text');

            if (demoContent.style.display === 'none') {
                // 段階1: 絞り込む → 絞り込み中（青・pulse）
                this.classList.add('filtering');
                filterText.textContent = 'AI三連複 絞り込み中';

                // 段階2: 1.6秒後に 絞り込み中 → 絞り込み完了（緑）
                setTimeout(() => {
                    this.classList.remove('filtering');
                    this.classList.add('filtered');
                    filterText.textContent = 'AI三連複 絞り込み完了';
                    demoContent.style.display = 'block';

                    // スムーズスクロール
                    setTimeout(() => {
                        demoContent.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }, 100);
                }, 1600);
            } else {
                // 閉じる処理
                demoContent.style.display = 'none';
                this.classList.remove('filtered');
                filterText.textContent = 'AI三連複 絞り込む';
            }
        });
    }
});
</script>
